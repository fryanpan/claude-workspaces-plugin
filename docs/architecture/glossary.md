# Glossary: one name per concept

The product and the code have drifted into two vocabularies: the UI says Home, Tasks, Activity and Plan, while the code says rooms, redline and LF. This file names one word per concept, records the decided name, and says where the code still disagrees so the rename pass has one list to work from.

## Concepts

| Product name (as the UI says it)                             | Current code names                                           | Decided name                                                 | Cost | Notes                                                        |
| ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ | ---- | ------------------------------------------------------------ |
| **Workspace**                                                | `packages/server/src/workspace-store.ts`, `routes/workspaces.ts`, `/workspaces`, `create_workspace` | workspace                                                    | —    | Already aligned. The container; keep it as the outermost noun. |
| **Board** ("← Back to the board", "this board's asks")       | `packages/workspaces-app/src/board/` (`board-app.ts`, `board-render.ts`, `board-model.ts`, `board-island.tsx`, `board-url.ts`), `BoardPane = 'board'` | board                                                        | L    | **Done 2026-09-05.** `hub/` → `board/`, `Hub*` → `Board*`, `hub-` CSS classes → `board-`, `hub.css` → `board.css`; the URL still reads `/workspaces/{id}` because the workspace is the container. |
| **Home** (nav label, `/workspaces/:id/home`)                 | `board/home-clock.ts`, `home-review-island.tsx`, `home-activity-island.tsx`, `server/src/home-brief.ts`, `doc-origin-repo.ts` | home                                                         | S    | **Done 2026-09-05.** Home keeps its name; `DocHome` / `doc-home.ts` are `DocOriginRepo` / `doc-origin-repo.ts`, with `resolveOriginRepoCheckout`, `verifyPathInOriginRepo` and `originRepoGuard` beside them. The persisted `docHome` meta key stayed — it is written into each pinned doc's `.private.json` sidecar. |
| **Tasks** / **My Tasks** (nav labels)                        | `HubNav = 'tasks' \| 'mine'`, `server/src/tasks.ts`, `task-*.ts` (20 modules), `routes/tasks*.ts`, `core/src/task-wire.ts` | task                                                         | —    | Aligned. `mine` is the nav key for My Tasks; fine as a URL segment. |
| **Activity** (nav label, "Recent activity")                  | `server/src/activity.ts`, `activity-backfill.ts`, `activity-lock.ts`, `board/activity-model.ts` | activity                                                     | —    | Aligned.                                                     |
| **Goal** ("Goal", "Projected")                               | `server/src/task-goals.ts`, `routes/workspace-goals.ts`, `core/src/goal-effort.ts`, `goal-summary.ts`, `board/goal-detail-island.tsx` | goal                                                         | —    | Aligned. "Goal band" appears only in tests, never in `src`.  |
| **Review item** ("For Your Review", "What I need from you", "Needs your reply") | `core/src/review-item.ts`, `server/src/review-items/`, `review-queue.ts`, `board/board-review-model.ts`, `review-item-phrase.ts`, `add_review_item` | review item                                                  | —    | Aligned. The other three "review" senses are retired under Decision 2; review means this queue only. |
| **Decision** (UI "Decision", "Answer")                       | `core` `ReviewShape = 'decision'`, `server/src/review-items/decisions.ts`, `decision-shape.ts`, `answer_decision` | decision                                                     | —    | Aligned. A decision is a shape of review item, not a sibling concept. |
| **Document editor** (`/review/<docId>`)                      | `packages/workspaces-app` (package `@feedback/workspaces-app`), `editor.ts`, `router.ts`, `review-surface.ts`, `review-chrome.ts` | document editor; package workspaces-app; route /workspaces/{id}/docs/{docId} | M    | **Package done 2026-09-03** (`markdown-app` → `workspaces-app`, PR #629) — ahead of this lane, and this row went unmarked until now. **Done 2026-09-06.** `/workspaces/{id}/docs/{docId}` is the only address: the page, the JSON record (`?format=json`), the ~30 subroutes and the live socket (`/y`) all answer under it, and `/review/<docId>` is DELETED — no redirect, and the client parsers that recognised it were deleted with it. |
| **Attachment** (a doc, mockup, preview or diff a board holds for comment; UI today says "review docs", "Back to all review docs") | `core/src/attachment.ts` `attachmentIdOf`, `server/src/binds.ts`, `/api/reviews`, `create_diff_review`, `archive_attachment_set`, `attach_doc` | attachment; a set of them is an attachment set               | M    | **Names done 2026-09-05.** "Review" means only the person-facing queue, and no user-visible string says review for an attachment. Kinds: document, mockup, preview (dev server), diff. **Route done 2026-09-07:** the collection answers at /workspaces/{id}/attachments — the word this glossary argues for, in the URL as well as the code. The segment was `reviews` until then; it moved on its own, after the canonical-routes lane, because it is a rename rather than a reshape. No old-path support, on the same call as the cutover. |
| **Diff review**                                              | `server/src/git-diff.ts`, `diff-groups.ts`, CSS `DIFF REVIEW SURFACE`, `workspaces-app/src/diff-nav.ts`, `create_diff_review` | diff review                                                  | —    | Aligned.                                                     |
| **Code review surface**                                      | `workspaces-app/src/code/` (`code-app.ts`, `code-editor.ts`), CSS `CODE REVIEW SURFACE` | code surface                                                 | —    | Aligned.                                                     |
| **Suggestion** ("Suggestion", "Accept", "Reject", "Revised") | `workspaces-app/src/suggestions/` (`suggestion-chips.ts`, `suggestions-summary.ts`), `core/src/redline.ts`, `core/src/suggest.ts`, `suggest-marks.ts`, `accept_suggestion` | suggestion                                                   | M    | "Redline" is never shown to a user. Redline is a separate concept, the change view (`workspaces-app/src/redline/`) that renders suggestions and git diffs; `suggestion-chips.ts`/`suggestions-summary.ts` moved out of it into `suggestions/` on 2026-09-05 (Decision 5, done — see rename order step 6.1). Decision 5 keeps both names. |
| **Comment thread** ("Comments", "Open thread", "Resolve")    | `core` `Thread`/`ThreadSummary`, `workspaces-app/src/threads.ts`, `thread-modal.ts`, `server/src/thread-request-dedup.ts`, `create_thread` | thread                                                       | —    | Aligned.                                                     |
| **Plan / Meeting notes** (the UI says "Plan", "Meeting notes", "Make a plan", "Have a meeting") | `core` `HuddleKind = 'plan' \| 'discussion'`, `docKindLabel`, `server/src/huddle.ts`, `workspaces-app/src/huddle-entry.ts` | plan, meeting notes; button "Have a meeting"                 | S    | **Words done, identifiers pending.** "Huddle" left the UI on 2026-09-02: doc titles, the editor crumb and the Board button now read Plan / Meeting notes, and `docKindLabel` in `core/src/types.ts` is the one place the word is chosen. Everything underneath still says huddle — `HuddleKind`, `huddle.ts`, `huddle-entry.ts`, the `huddle` / `huddleKind` meta fields, the `.doc-label-huddle` and `.board-huddle-start` class names, the `?huddle=1` param, the `POST /workspaces/:id/huddles` route, the `huddle-…` doc aliases and the `huddles/` data directory. Those are the rename lane's work, not a user-visible bug. |
| **Meeting** / live transcript / meeting notes                | `server/src/meeting-*.ts` (12 modules), `meetings.ts`, `core/src/meeting.ts`, `workspaces-app/src/meeting-*.ts` | meeting                                                      | —    | Aligned.                                                     |
| **Recording** ("Start recording", "Record Audio", "Recording options") | `workspaces-app/src/meeting-audio.ts`, `voice-capture.ts`, `core` `CaptureMode` | recording                                                    | S    | Code says capture, UI says recording. Low value, rename with the meeting lane. |
| **Speakers** ("Who said this", "Tap to name this speaker")   | `core/src/speaker-roster.ts`, `speaker-tags.ts`, `workspaces-app/src/speaker-reassign.ts`, `server/src/voice-resolve.ts` | speaker                                                      | —    | Aligned.                                                     |
| **Feedback widget**                                          | `packages/widget` (`@feedback/widget`), `FeedbackWidgetEl`, `server/src/mockup-widget.ts`, `widget-auth-page.ts`, `/widget` | feedback widget                                              | —    | Aligned and the one place `feedback` is still the right word. |
| **Mockup**                                                   | `server/src/mockup-capture.ts`, `mockup-widget.ts`, `/mockup`, `core` `DocType = 'mockup'`, `attach_mockup` | mockup                                                       | —    | Aligned. The MCP verb became `attach_mockup` on 2026-09-04 (Decision 10); the name it had before still dispatches for one release. |
| **Share / collaborator** ("Sign in", "Commenting as")        | `server/src/share/` (`shares.ts`, `sharing-gate.ts`, `visitor-identity.ts`), `/api/share`, `share_workspace` | share                                                        | —    | Aligned.                                                     |
| **Sign-in** (`/signin`, "Sign in to comment or edit")        | `workspaces-app/src/signin/`, `server/src/auth/`, CSS `SIGN-IN PAGE` | sign-in                                                      | —    | Aligned.                                                     |
| **Agent** / **lead agent** ("Assign a lead…", "No lead agent") | `server/src/task-agents.ts`, `lead-presence.ts`, `dispatch-registry.ts`, `attach_agent`, `set_workspace_lead` | agent                                                        | —    | Aligned.                                                     |
| **Dispatch** (not user-visible)                              | `server/src/dispatch-registry.ts`, `/api/dispatches`, `register_dispatch`, `close_dispatch` | dispatch                                                     | —    | Agent-facing only; no product word needed.                   |
| **Plan** ("Make Plan", "Approve Plan", "Draft — held until the plan is approved") | `workspaces-app/src/plan-gate.ts`, `plan-placeholder.ts`, `core` `HuddleKind='plan'` | plan                                                         | —    | Aligned.                                                     |
| **Spin off a task** ("Turn this line into work")             | `core/src/spinoff.ts`, `workspaces-app/src/spinoff-menu.ts`, `spinoff-link.ts`, `spin_off_task` | spin-off-task                                                | —    | Aligned. The MCP verb became `spin_off_task` on 2026-09-04, the same words as the menu item; the name it had before still dispatches for one release. |
| **Archived / Deleted** (filter labels)                       | `server/src/review-archive.ts`, `archive_task`, `archive_workspace`, `delete_doc` | archive / delete                                             | —    | Aligned. Two words only: `archive_workspace` stands a board down reversibly (renamed 2026-09-04, old name aliased for one release), and delete stays the destructive verb. |
| **Held** (review-item kicker in the task panel)              | `core` `ReviewJudgeVerdictKind = 'held'`, `server/src/review-judge.ts`, `review-items/judgements.ts` | held                                                         | —    | Aligned. A judgement verdict, unrelated to the removed park state. |

## Code names with no product concept

| Code name                                                    | What it actually is                                          | Proposal                                                     |
| ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
| `markdown-app/src/hub/` (and `hub-app`, `hub-render`, `hub-actions`, `HubNav`, `HubPane`) | The whole board application, including Home and Activity     | **Done 2026-09-05.** `hub/` is `board/` and `HubNav`/`HubPane` are `BoardNav`/`BoardPane` (Decision 1); the package it lives in is itself `workspaces-app` (see the `packages/markdown-app` row below). |
| `server/src/rooms.ts`, `Rooms`, `RoomsConfig`, `/rooms`, `room-timings.ts` | The document store: every bound doc, its CRDT, its threads   | **Done 2026-09-05.** The store is `DocStore` in `doc-store.ts`, with `DocStoreConfig`, `doc-store-timings.ts` and `doc-store-workspaces.ts` beside it, and every local that holds one reads `docStore`. "Room" survives only where it names the Yjs/WebSocket protocol object — `DocRoom`, `room-fanout.ts` and the singular `room` helpers that take one — and on the `rooms` count `GET /api/metrics` publishes, which counts hydrated rooms rather than stored docs |
| `markdown-app/src/redline/`                                  | The suggestion rendering surface                             | **Done 2026-09-05.** Kept as the Redline change view; `suggestion-chips.ts` and `suggestions-summary.ts` moved into `workspaces-app/src/suggestions/` |
| The old env and CSS class prefixes taken from the "live feedback" name | Residue of the "live feedback" name                          | **Done 2026-09-04.** Env names are `CW_*` and the alias table in `core/src/env-names.ts` is empty; CSS classes, `data-*` attributes, storage keys and test scratch prefixes all read `cw-`. One value was left alone: `HUB_FEEDBACK_DOC_ID` names a doc that already exists in the corpus, so it needs a migration rather than a rename |
| `@feedback/core`, `@feedback/server`, `@feedback/markdown-app` | The npm scope of the workspace packages                      | **Done 2026-09-07.** All six workspace packages read `@claude-workspaces/*`, matching `@fryanpan/claude-workspaces-mcp`. Purely internal: the scope names nothing published and appears in no user-visible string. |
| `packages/markdown-app`                                      | Four surfaces: markdown, code, diff, mockup                  | **Done 2026-09-03** (PR #629, Decision 3) — the directory and the package name are `workspaces-app`; the npm scope followed on 2026-09-07 and the package is `@claude-workspaces/workspaces-app`, alongside the other packages in that row below. |
| `server/src/shells.ts`                                       | The five server-rendered HTML pages                          | `pages.ts`                                                   |
| `server/src/keep-moving.ts`, `stall-gate.ts`, `stall-nudge.ts`, `ready-gate.ts`, `ready-nudge.ts` | Stall detection and board wakes                              | Group under `stall/`; "keep-moving" is a protocol name, not a component name |
| `server/src/binds.ts`, `bindFolder`, `bindDiff`               | Attaching a file, folder or mockup to a workspace's attachment set | **Verbs done 2026-09-04.** With Attachment as the product word, attach won: the MCP surface is `attach_folder`, `attach_mockup` and `attach_doc`, each pre-rename name aliased for one release. The server module still says bind |
| `core/src/anchor/`, `SubjectAnchor`, `OrphanAnchor`          | Where a comment is pinned, and what happens when the text moves | Keep. Internal, coherent, no product word competes           |

## Decisions for Bryan

1. **hub vs board** — the directory said `hub`, the UI says "board", and the URL says `/workspaces/:id`. 
  1. Decided (Bryan, 2026-09-02: "Board is good"): the screen is the Board; rename the hub/ directory and hub-* modules to board in the rename pass; the URL keeps /workspaces/:id because the workspace is the container.
  2. **Shipped 2026-09-05** — see rename order step 5.
2. **The four senses of "review"** — review item (a person's queue), review set (a folder or diff bind), review doc (a bound markdown file), and the document editor route, which was `/review/<docId>` and is now `/workspaces/{id}/docs/{docId}`. 
  1. Decided (Bryan, 2026-09-02): "review" means only the person-facing queue (review item).
  2. Everything a board holds for people to comment on — doc, mockup, preview (dev server), diff — is an "Attachment"; 
  3. a review set becomes an attachment set, and URL paths follow the Google API design guide (plural collection names, `/collection/{id}` nesting): 
  4. The /review/<docId> editor route becomes /workspaces/{id}/docs/{docId}, matching the existing /workspaces/{id}. Decided (Bryan, 2026-09-02). Done 2026-09-06.
    1. All attachments are owned by a workspace so ownership and permissions are clearSo the URL structure should reflect that going forward
  5. **Names shipped 2026-09-05; the routes are half-shipped** — the board's own routes and two name collisions cleared 2026-09-05, the doc/task/attachment-set collections are still open — see rename order step 6.
3. `packages/markdown-app`
  1. Rename to `workspaces-app` (the whole browser client: editor, Home and board, sign-in, landing), rename to `editor`, or leave it and accept that the code surface and diff surface live inside a package named for markdown?
  2. Bryan: Rename to `workspaces-app` 
  3. **Shipped 2026-09-03** (PR #629) — the directory and package name moved ahead of the rest of this lane; this decision's own "shipped" note went missing until this pass. The `@feedback/*` npm scope followed on 2026-09-07 (see the code-names table).
4. **`DocHome`** — the type means a doc's repo of origin, which reads as the Home page. 
  1. Rename to DocOriginRepo (Bryan, 2026-09-02); the Home page keeps its name.
  2. **Shipped 2026-09-05** — see rename order step 6.1.
5. **redline vs suggest** — two distinct product concepts, not one. Suggestion is the proposal made in Suggesting mode (Accept / Reject). Redline is the change view: struck deletions, marked insertions and margin balloons, used to show suggestions and, through the same diff algebra in core/src/redline.ts, git diffs in a diff review. Decided (Bryan, 2026-09-02): keep both names; the only rename is moving the suggestion UI files (suggestion-chips, suggestions-summary) out of the redline/ folder into suggestions/. **Shipped 2026-09-05** — see rename order step 6.1.
6. **Huddle** — the UI mostly said "Have a discussion" and "Make a plan" and only called the artifact a Huddle. 
  1. Decided (Bryan, 2026-09-02): drop "Huddle" from the UI; the two kinds are Plan and Meeting notes, and the button becomes "Have a meeting". Code may keep huddle as the shared container until the rename pass.
  2. **Shipped 2026-09-02, words only.** No user-visible string says Huddle any more. The identifiers listed in the concept table above were deliberately left alone so the rename lane can move them in one pass rather than in a copy-edit.
7. **`park_task`** — the `parked` task state was removed in August 2026 and the verb now means "move to triage and leave a note" (`server/src/park-note.ts`, `park-migration.ts`). 
  1. Decided (Bryan, 2026-09-02): triage is confusing as a parking place; add a Blocked state meaning "waiting on a dependency or a date", park_task becomes block_task and moves a task there with the reason. Filed as its own task.

## Suggested rename order

Renames run **after** the current file-split lanes land, so nothing here competes with a lane's diff.

1. **Done 2026-09-04.** The CSS class prefix, the `data-` attribute prefix, the browser storage keys and the test scratch-dir prefixes all read `cw-`, and the legacy env aliases are gone. The single exception is `HUB_FEEDBACK_DOC_ID`, whose value names a live doc in the corpus. Ticket: "CSS and env prefix" ticket.
2. **Done 2026-09-04.** The MCP verbs read `attach_mockup`, `attach_folder`, `spin_off_task` and `archive_workspace`; each keeps the name it had as an alias for one release, which says so once per session in the MCP log. Ticket: "MCP verbs" ticket.
3. park_task → block_task and a Blocked task state that replaces Triage (Decision 7, decided: blocked means waiting on a dependency or a date). One MCP verb and two server modules whose name outlived the state they described. Tickets: "Blocked state" ticket, then "MCP verbs" ticket.
4. **Done 2026-09-05.** `rooms.ts` is `doc-store.ts` and `Rooms` is `DocStore`, along with `RoomsConfig`, `rooms-workspaces.ts`, `room-timings.ts`, `makeRooms`, `isHubOwnedRoom` and every `rooms` local across the server and its suites. Purely internal: no REST path, MCP tool name or response field moved. Ticket: "DocStore" ticket.
5. **Done 2026-09-05.** hub/ → board/ and Hub* → Board* (Decision 1, decided): `markdown-app/src/hub/` is `workspaces-app/src/board/`, `HubNav`/`HubPane` are `BoardNav`/`BoardPane`, and every `hub-` CSS class and `hub.css` moved to `board-`/`board.css`. Touched roughly thirty modules and the CSS. Ticket: "board/" ticket.
6. review → attachment (Decision 2, decided). Split in two: the names half has landed in full, and the routes half has landed in part.
  1. **Names done 2026-09-05.** A review set is an ATTACHMENT SET in the code and in every user-visible string: `core/src/review.ts` is `attachment.ts` with `reviewIdOf` → `attachmentIdOf` and `isReviewMember` → `isAttachmentMember`, `server/src/review-backfill.ts` is `attachment-backfill.ts`, "Back to all review docs" is "Back to all attachments", the landing fold reads "Attachments by project", and the doc-list sidebar and its CSS banner say attachment set. `DocHome` is `DocOriginRepo` in `doc-origin-repo.ts` (Decision 4), and `suggestion-chips.ts` and `suggestions-summary.ts` left `redline/` for `suggestions/` (Decision 5) — covered by `workspaces-app/test/suggestions-outside-redline.test.ts`, which renders both on the plain markdown surface with no redline module loaded. "Review" now means only a person's queue of review items. Nothing moved on the wire: `setId`, the `reviewId` bind field, the `docHome` meta key, the `reviewUrl` listing field and every URL path stayed put. Ticket: "attachments" ticket.
  2. **Every route is canonical (2026-09-06).** The `/api` prefix is gone from every resource route: a board, its goals, tasks, docs, mockups, reviews, review items and dispatches all answer under `/workspaces/{id}/…`, HTML by default and the record with `?format=json`. Only sign-in, the landing page, deploy, the share entry, static assets and the two external callbacks (Recall webhook, Google OAuth redirect) stay top-level, alongside the agent identity family (`/api/agents/{id}/token|watches|merge`, `/events/agent/{id}`), which is wider than any one board and therefore has no board-scoped address — the agent's turn and denial NOTES are not in that family: they moved to `/workspaces/{id}/agents/{name}/notes` on 2026-09-06 (owner: "Still move it"), with the board read from `CW_WORKSPACE_ID` by the hooks and by a bare `post_status`. **One middleware resolves the board** (`middleware/workspace-scope.ts`), above every handler: it answers whether the board exists and whether the row named under it belongs to that board, refusing both with 404 — never 403, because a 403 on a foreign id confirms the id is real. Every per-route workspace check it replaces was DELETED, not left dormant, and a table test walks one probe per collection (keyed off the middleware's own `SCOPED_COLLECTIONS`, so a new collection with no probe fails the suite) proving a foreign board id 404s on all of them. **No old-path support:** the previous addresses were deleted in the same change that moved every caller — no redirects, no 410s, no dual-prefix aliases — and `/review/<docId>` and `/mockup/<docId>` went with them, parsers included. The prerequisite that used to block this was a decision, and it was taken: **`workspaceId` is a required argument on the ~59 MCP tools that addressed a bare id**, and the widget takes a required `workspace-id` attribute that fails loudly when absent. The share allowlist was re-derived for the merged prefix — a board's pages and a board's data are one table now, and a row verb asks two questions (the segment is the shared board AND the row is inside it). The two name collisions were cleared first and separately on 2026-09-05: the attached-agent roster left /workspaces/{id}/attachments for **/workspaces/{id}/agents**, freeing `attachments` for the noun this glossary spends it on, and the board's live feed left /events/workspace/{id} for **/workspaces/{id}/events:stream**, leaving `events` to the activity feed it collided with. Two grouped with this lane shipped on their own schedule: markdown-app → workspaces-app landed 2026-09-03 (PR #629), and the Huddle strings → Plan / Meeting notes (Decision 6) on 2026-09-02. The @feedback/* scope → @claude-workspaces/* (Decision 3) landed 2026-09-07, and the `reviews` segment → `attachments` with it. Nothing is open in this lane. Tickets: "canonical routes" ticket, "workspaces-app" ticket, "Plans and Meeting notes" ticket.
