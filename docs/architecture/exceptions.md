# Files over 500 lines

The bar: **every file over 500 lines has either been broken up, or has an
explicit exception written here.** This page is the record of that decision, one
row per file. `bun run loc:audit` fails when a file crosses the limit without a
row, and CI runs it alongside the other gates.

A `Split` verdict names a seam — which functions become which new file — and is
work that is queued, not done. The audit does not enforce verdicts, only that
every oversized file has one: a listed `Split` passes. The point of the gate is
that a **new** god file cannot appear with nobody writing down why.

Regenerate the raw list with:

```bash
find packages \( -name '*.ts' -o -name '*.css' \) \
  -not -path '*/node_modules/*' -not -path '*/dist/*' \
  | xargs wc -l | awk '$1>500' | sort -rn
```

Audited 2026-09-02 at `3a39db67`, and re-counted 2026-09-03 with the command
above: **161 files** over 500 lines, 68 source and 93 test. (The count written
here on 2026-09-02 said 164/70/94; two of that difference predates this
re-count and is not attributable to a listed split.) A7 took `activity.ts`
(582 → 301) and `recall-calendar.ts` (721 → 470) off this list, and A8 took
`voice-smooth.test.ts` (729 → 493); all three rows are gone rather than marked
done. B5 took `prose.ts` (2847 → 83) and `goal-effort.ts` (1086 → 333) off it
the same way, and added three rows for the files that came out of `prose.ts`.
B6 shrank `mcp.ts` 5,563 → 1,426, which cleared the seam its row named without
clearing the bar; the coverage extraction then took it to 400 and its row is
gone rather than marked done. Neither of the two rounds put a new file over the
limit: the eight modules lifted out of `mcp.ts` for coverage are 89–459 lines
and none needs a row.

Two rows were added on 2026-09-03, taking the list to **163**, when the app's
stylesheet tests stopped grepping `styles.css` and started installing it. A
rule that used to be one `toMatch` line became a mount, a viewport and a
computed read, so `md-composer.test.ts` and `hub-mic-dock-css.test.ts` crossed
the line without gaining a second subject.

One more was added later on 2026-09-03 for `share-link-flow.test.ts`, the new
share-link route suite. It is a first draft over the limit rather than a file
that grew into it: the flow it drives needs two Access audiences and two
hostnames configured on one server, and every question worth asking about a
share link is a question about that one fixture.

A10 then split `rooms.ts` (4801 → 3490, and 3527 once the share-link work
landed 37 lines of websocket context in it) and added one row for
`file-binding.ts` (1652), the seam the `rooms.ts` row had named. A split that
lifts an oversized subject out of an oversized file adds to this list rather
than shortening it — what changes is that each row now describes one thing.

A11 split `hub-app.ts` (2592 → 2166) into three modules that are each under
the limit — `hub-shell.ts` (193), `hub-settings-panel.ts` (181) and
`hub-walkthrough.ts` (271) — so this one shortens the list by nothing and
lengthens it by nothing. That is the shape to expect from a file whose
remaining mass is a render closure: what comes out is the pieces that did not
need it, and the row that stays gets shorter rather than disappearing.

A12 then took `server.ts` from 7531 to 5934 across seven modules, of which
only one — `review-gate.ts` (783) — is over the limit and gains a row. The
other six are 133–366 lines and need none. The lesson worth keeping is in the
row this slice was working from: it named `server-deps.ts` as the file to
create, and `server-deps.ts` already existed, doing the OTHER half of the
composition root (the adapters `bin.ts` builds to talk out). A verdict written
months ago is a hypothesis about the code, not a description of it — check the
seam before you cut it.

A13 then took `hub-app.ts` from 2166 to 792, by doing the thing A11's row said
was not worth doing. A11 called the render closure one subject because every
`render*` shared one `state`, and read the detail panel's thirty-plus deps as
proof that the seam was the whole closure passed as an argument. That reading
was wrong in one specific way: a long dependency list is not the same as an
implicit one. Written out, `HubDetailDeps` is the panel's whole reach — it is
long because the panel is where a ticket's every verb is, and what the list
buys is that the panel can no longer touch anything absent from it. Twelve
modules came out on that principle (`hub-islands`, `hub-projection`,
`hub-board-region`, `hub-queue-open`, `hub-home-region`, `hub-discussion`,
`hub-detail-panel`, `hub-chrome-region`, `hub-data-loads`, `hub-load-report`,
`hub-deep-links`, `hub-voice`, plus `wireNavCollapse` into `hub-shell.ts` and
`wireHubShortcuts` into `hub-shortcuts.ts`); only one of them —
`hub-detail-panel` (572) — is over the limit and gains a row, so the list
grows by one while the row it leaves behind describes a boot sequence rather
than a render layer. The lesson to keep: **"they all share one `state`"
argues for one PROJECTION, not for one file.** A region that takes
that projection plus a named deps object is smaller than the closure it came
from AND says what it may reach, which the closure never did.

A14 then took `server.ts` from 5934 to 5358, moving the stall / ready-work
wiring out whole to `stall-wiring.ts` (935, and over the limit, so the list
grows by one). The 754 lines that moved are byte-identical to what left apart
from six edits, all named in the PR: five `opts.` reads that became context
fields and one `let` assignment that became a returned `const`. The lesson to
keep is about the ordering the A12 row flagged: `hubBoardsForDoc` really is
built below this block, and so are `backTargetFor` and `reviseCallFor` — and
none of that mattered, because every one of them is called from a request or
an event rather than during composition. **A declaration-order problem that
only exists at call time is a THUNK, not a reordering.** Hoisting the review
gate to satisfy it would have traded this constraint for a worse one: the gate
sits where it does because it needs `resolveWorkspaceForDoc` and the task
projection.

`app.ts` then came off the list entirely (1569 → 484). Its row named the seam
as "a sequence rather than a set of jobs", which was true of the ORDER and not
of the work: each step of that order — the balloon margin, the two floats, the
selection pills, the spin-off runner, the sidebar, the save-state chip, the
write gates — turned out to take a handful of named arguments rather than the
whole closure. Nine modules under `src/doc/`, none over 500, so this one
shortens the list by one and lengthens it by nothing. What is left in `app.ts`
is the sequence that calls them, plus the forward refs the editor's callbacks
need and the reads of the address the mount was opened at.
Test files are judged by a narrower rule, in their own table below: a long test
file is an exception unless two *unrelated harnesses* share it. Many `describe`
blocks over one set of fixtures is one harness, however long the file gets.

---

## packages/server

| File | Lines | Verdict | Reason / seam |
|---|---|---|---|
| `packages/server/src/server.ts` | 3300 | Keep | A1 took five families out; A12 took the composition; A14 took the stall / ready-work wiring out whole to `stall-wiring.ts` (938). A15 took the seam the A14 row named — the doc↔board membership map, out whole to `board-membership.ts` (803). What moved is one question asked from four directions: which boards hold an id (`hubWorkspacesHolding` and the prebuilt-index twins `boardIndexForListing` / `heldByIndexed` / `hubBoardsForDocIndexed` / `homeForDocIndexed`), which boards a DOC reaches (`hubBoardsForDoc`, `backTargetFor`), who may reach it through one (`shareWorkspacesOf`, `collabMemberOf`, `shareLinkMemberOf`, `redeemShareLink`), and whether an agent's watch set covers them (`watchCoverageFor`, `queuedForLead`) — plus the three verbs that WRITE the links all of that reads (`fileUnderHubWorkspace`, `unfileFromDefault`, `unlinkFromEveryHubWorkspace`), `defaultHubWorkspaceId` and the four `Coverage*` / `WatchCoverage` types. Those types were also re-exported from `server.ts`, on the theory that it was the module name they had always been importable under; A16 checked every importer and dropped the re-export, because nothing reads them from `server.ts` — `packages/mcp` carries its own copies of the shapes and the server's own coverage test declares local ones. The A14 row's prediction held exactly: because nothing the map reads is built later than the stores, `createServer` composes it ABOVE the stall wiring and two of that wiring's three thunks (`hubBoardsForDoc`, `backTargetFor`) are now plain values. `reviseCallFor` stays a function — the review gate is still built below, for the reason it says where it is built. Nothing else inside `createServer` was reordered — though the move did take one name out of the closure entirely: `DEFAULT_HUB_WORKSPACE_NAME` was a `const` declared inside `createServer` and is now a module-level export of `board-membership.ts`, which is where `routes/auth-share.ts` and `server.ts` both read it from. A16 took the seam that sentence named — the Home pane, out whole to `home-pane.ts` (308). What moved is one question asked twice: what one person sees on one board's Home tab (`homeBriefs`, `reviewItemsFor`, `homePayload`) and what the brief may CLAIM about it (`homeQueueTotal`, `homeBriefInput`, `homeBriefInflight`, `generateHomeBriefFor`). `reviewItemsFor` is why those two halves stay in one file: the queue it builds is both the list `GET /review-items` renders and the denominator `homeQueueTotal` prints, so splitting them would be two readings of "what is waiting on me" with nothing asserting they agree — the same failure shape the `board-membership.ts` row refuses for coverage. The A15 row's prediction held: every dependency (`dataDir`, `taskStore`, `rooms`, the summarizer seam) is built above the composition point, so the context is four values and nothing is deferred. `taskBodyDocId` is imported directly rather than passed — it is a free function, not a method on the projection. The bodies moved byte-identical, comments included; what is new is the context interface and the `HomePayload` shape, which used to be inferred. `homeBriefInflight`, `homeBriefInput` and `generateHomeBriefFor` stayed internal — nothing outside reached them inside `createServer` either, and an in-flight set is only correct if exactly one thing owns it. A17 took the seam that sentence named — the request-admission run, out of the top of `fetch` to `request-admission.ts` (673). What moved is the default-deny host gate, the auth branch its decision selects (share, share-link, collab, the recall callback, proxied-local, and the legacy whole-server Access mode), the external-access master switch, the share-visitor scoping, and — one layer out, same question — the origin policy `applyCors` and the cross-origin write gate both read, with `canonicalOrigin` following the only caller it had. The row's warning is what shaped it. This family is PER-REQUEST, so it could not come out as a factory of values the way A14–A16 did, and the obvious return — a record carrying a response AND the values — is precisely the tuple hazard predicted here: a caller reads `visitor` off a request the gate refused. So the result is a DISCRIMINATED UNION. `{ admitted: false, response }` carries no per-request value at all and `{ admitted: true, visitor, visitorShareId, visitorMemberKey, accessEmail, metaFor }` carries every one, which makes the hazard a compile error instead of a review note. `admitted: false` means the gate ANSWERED — the share-link redeem route lives inside the gate and its 200 comes back the same way a 403 does. The decision moved without being retyped: every refusal still reads `return j(...)`, inside one wrapper whose null return means admitted, so exactly one place turns an answer into `admitted: false` and no branch added later can refuse and forget to say so. Order is unchanged — the attribution helpers that read these values (`provenIdentityFor`, `authorFor`, `stampRosterAgent`, `withTaskChips`) stay above the gate call, closing over consts declared below them exactly as they closed over the `let`s before; `metaFor` is the one that moved, onto the admitted branch, because it is a function of the resolved `visitor` and nothing else. A18 took the seam that sentence named — shell and static serving, out to `shell-static.ts` (627), and the prediction held: everything in it is decided AFTER admission and its only per-request input is the `visitor` the gate resolved, so it came out as a factory of long-lived values with the visitor passed per call. Two halves moved together — the closure helpers (`addressableWorkspaceFor`, `reviewEntryDocId`, `serveDocShell`, `serveShellHtml`, `isMockupDoc`, `serveMockup`) and the run of router blocks they serve, from the widget statics through the root-path app files, the workspace hub, the workspace-scoped resources, the markdown app, mockup HTML, demos, the sign-in page, the landing page and a project's artifacts, with `ROOT_ALIASED_ASSETS` following the one route that reads it. The fall-through is what made this a clean tail extraction: `serveShellRoutes` returns `Response | null`, null means no block claimed the address — exactly what the run did in place when a static file was missing — and `createServer` answers a null with the 404 that has always sat at the bottom of the handler. One substitution across the router run's 293 lines: `DEFAULT_HUB_WORKSPACE_NAME` became an injected context member, so this module does not depend on `board-membership.ts` for a string. A18 also put the new file on the `ship-it` security trigger, against the expectation that it holds no auth surface. It does not, and that was not the test: the checklist asks you to name the gate on every MOVED route, to say what rejects a hostile file path, and whether share scope widened — and this file holds the three static roots, their containment checks, a mockup served from an absolute bound path, and `/widget/`, which is on the share-host allowlist. The next seam is the UPGRADE-AND-STREAM run: the three websocket upgrades (Recall's bot audio, a doc's live meeting audio, and `/y/<docId>` editing) and the three SSE openers (agent-level, workspace-level, per-doc), about 230 contiguous lines answering one question — this request wants a long-lived connection rather than a response, so which one, and stamped with what so revocation can find and close it later. Same proven shape as A18: a contiguous router run whose per-request inputs are the address and the admitted visitor. After that, the one genuinely interesting seam left is per-request ATTRIBUTION — `provenIdentityFor`, `authorFor`, `stampRosterAgent`, `refuseCategoryAuthor`, `withTaskChips`, the widget-token gate and `browserProvedNobody`. It is the other half of the question A17 answered: admission decides who the boundary PROVED, attribution decides whose name goes on the write. It is per-request like A17 and harder than A18 for that reason — it would take the admission result as an input, so the two per-request modules chain rather than compose. A19 took the seam that paragraph named — the upgrade-and-stream run, out to `upgrade-stream.ts` (439). Six blocks answering one question: three websocket upgrades (Recall's bot audio on an unguessable per-bot token, a doc's live meeting audio, `/y/<docId>` editing) and three SSE openers (an agent's whole watch set, a workspace's thread events, one doc's), with `sseLastEventId` following the three calls that were its only readers and `UpgradeData` following the three blocks that construct it — `server.ts` now imports that type for `Bun.serve<UpgradeData>` and the handler that reads it back. The A18 shape held: a factory of long-lived values, the address and the admitted visitor per call. What is new is the RETURN. A18 could answer `Response | null` because each of its blocks either answered or declined; an upgrade has a third outcome that Bun spells as a hole in the type system, because a socket that takes the connection over means `fetch` returns `undefined`. `Response | undefined | null` puts three meanings across two nullish values, and the obvious `if (answered) return answered;` at the call site would read a live socket as a fall-through and answer the 404. So the boundary is `StreamOutcome` — null still means no block claimed the address, `upgraded` and `response` are named, and `createServer` is the one place that turns `upgraded` into the `undefined` Bun wants. The run inside is unchanged: it still returns a `Response` or `undefined`, inside a wrapper, so every gate keeps its position relative to the upgrade it guards — which is the property that matters here more than anywhere else, because a websocket is authorized ONCE and there is no later request to catch a gate that moved. The three `server.upgrade` calls moved with zero substitutions: the context takes `server` narrowed to that one method, as a forward reference the way A17 took `requestAddress`. Two dead things fell out and were removed rather than left: `route`'s `server` parameter and `fetch`'s, because those three upgrades were the only readers of it in the whole route table; and the orphaned `canonicalOrigin` doc comment A17 left stranded above `sseLastEventId`, which documented a function that had already moved to `request-admission.ts`. The `/recall/` upgrade still sits immediately below the bot status webhook, which stays in `server.ts` — the comment there calls that adjacency load-bearing, and it is ROUTING order that carries the property, so the call site holds the position the run held. The next seam is per-request ATTRIBUTION — `provenIdentityFor`, `authorFor`, `stampRosterAgent`, `refuseCategoryAuthor`, `withTaskChips`, the widget-token gate and `browserProvedNobody`. A19 makes the case for it sharper rather than weaker: `browserProvedNobody` is now passed into two modules per request, which is the shape of a family that wants its own boundary. It is the other half of the question A17 answered — admission decides who the boundary PROVED, attribution decides whose name goes on the write — and it chains with A17 rather than composing beside it, because its input is the admission result: it cannot be built before the gate has run, so it is a second per-request call taking the first one's output, not another factory in the same composition block. A20 took it, out to `request-attribution.ts` (346), and the chaining is what the slice is about: `AttributableRequest` is `Extract<Admission, { admitted: true }>` rather than a shape written out again, so adding a field to admission shows up here and renaming one stops this file compiling. The widget-token gate came with the family because it WRITES the state the family ranks — `widgetIdentity` is resolved once and read by `authorFor` at rung 0, and a gate left behind would make that field settable by anything and ranked here, which is the shape the precedence bug documented inside `authorFor` came out of. So the return is A17's union again: `attributed: false` carries no helpers, and no route can name an author on a request whose token was refused. All 190 moved lines are byte-identical under a normalized diff. Two things changed and neither is a value a helper sees: the helpers used to be declared ABOVE the gate call and read consts declared below them, safe only because nothing called them until admission had run, and taking the admitted result as an argument removes that ordering dependency; and `stampRosterAgent` stayed internal, because nothing outside reached it inside `fetch` either. `widgetIdentity` is on the boundary because `routes/auth-share.ts` reads it, and resolving it twice would be a second chance to disagree with `authorFor`. The sign-in write gate stayed in `createServer`: it CONSUMES `browserProvedNobody` rather than producing it, and it guards writes rather than naming authors. **Where the program should stop.** After A20 the remaining 3358 lines are no longer a route table with logic in it. They are, in order: 153 lines of imports; 695 of types, constants and module-scope helpers; 1364 of `createServer` building stores and composing the extracted modules; 77 of `Bun.serve` options and the `fetch` wrapper; 610 of `route()`, of which 230 are comment lines and which now holds 19 delegating call sites — 15 into the eight entry-point modules under `routes/`, and 4 into the extracted per-request runs (`admit`, `attributeRequest`, `serveUpgradeAndStreamRoutes`, `serveShellRoutes`); and 459 of websocket handlers, the share sweep, the park migration and shutdown. The mass has moved from the router to the COMPOSITION ROOT, and a composition root is supposed to be long and flat — it names every collaborator once, and splitting it buys indirection without reducing what a reader must hold at once. The route table at 610 lines is a dispatcher; the next split there would produce modules that are one `if` each. So: one coherent slice is left, and then this should stop. That slice is the WEBSOCKET HANDLER trio — `open`, `message` and `close`, about 160 lines — which is the natural partner of A19: A19 moved what decides a socket may open and what gets stamped on it, and this is what the socket then DOES with the stamp. It is coupled to shutdown, because the force-close drain in `stop` fires every `close(ws)` synchronously, so whatever moves has to keep that. After that the honest verdict on this row stops being Split and becomes Keep: a 3200-line file that is 40% composition root and 20% dispatcher is not a god file, and continuing to cut it would be motion rather than progress. A21 took that last slice — the websocket handler trio, out to `socket-handlers.ts` (152), all 84 lines byte-identical. `perMessageDeflate` came with them rather than staying behind as a bare option: it is a property of what these sockets do with their frames, and its measurement is about the very frame `open` sends first. The three stores in its context are passed as stores, never as values read out of them — a room is looked up per frame because `ws.data.docId` is re-resolved on every message and a room can be evicted or rehydrated while a socket is open, and hoisting any of it would bind every socket the server ever opens to the state that existed at boot. `close` stays SYNCHRONOUS and the module says why: `server.stop(true)` fires it inside that call, above `meetingRelay.dispose()` and `rooms.flush()`, because those handlers write and the stores receiving those writes have to still be live. That property now has its own test — the file already asserted the socket closes, which is what the CLIENT sees and passes even when our handler never runs, so A21 added the server-side assertion (the room's connection set, which only `onClose` empties) and proved it can fail: with `onClose` removed the new test fails and the old one still passes. **The verdict is now Keep, and this is where the program stops.** After twenty-one slices the 3300 lines are 153 of imports, 706 of types, constants and module-scope helpers, 1375 of `createServer` building stores and composing the extracted modules, 77 of `Bun.serve` options and the `fetch` wrapper, 610 of `route()`, and 380 of sweeps, shutdown and tail helpers. Two numbers decide it. The largest section is the COMPOSITION ROOT, which is supposed to be long and flat — it names every collaborator exactly once, and splitting it buys indirection without reducing what a reader must hold at once; the wiring is the thing you go there to read. And `route()` is now a dispatcher: 230 of its 610 lines are comments, and it holds 19 delegating call sites — 15 into the eight entry-point modules under `routes/` (which fan out across the 29 files in that directory), and 4 into the extracted per-request runs. The socket handlers, the fifth extracted run, are not among them: they are wired as a `Bun.serve` option, outside `route()` entirely. So the next cut here would produce modules that are one `if` each. What made the original 4841-line file a god file was that unrelated subjects were interleaved inside one function and could only be understood together. That is gone: every subject that had its own reason to change has its own file, and each one is named in this row with the seam it took and the seam it left. A long composition root plus a delegating router is not the same defect wearing a smaller number, and cutting further to move the number would trade a readable wiring diagram for a scavenger hunt. Revisit only if a NEW subject grows inside `route()` — the test is whether something in here has its own reason to change, not whether the line count went back up. **M** |
| `packages/server/src/board-membership.ts` | 803 | Exception | The doc↔board membership map, out of `createServer` in A15. One file because it is one question asked from four directions, and the two that look most separable are the two that must not be: the coverage readout (`watchCoverageFor`) reports gaps against exactly the board set `hubBoardsForDoc` fans events over, and the whole incident it exists to end is a probe answering "covered" about a board the events never reach. Two files would be two readings of "which boards" with nothing asserting they agree, and the drift would be invisible in that direction. The share-scope predicates are here for the same reason — `shareWorkspacesOf` IS the membership map read for a different purpose, and one function deliberately tells the allowlist that a review belongs to a hub AND that its members do. The three filing verbs came along because they WRITE the links everything above reads; the 403-on-your-own-share bug this file's comments record was a disagreement between the write side and the read side. About 130 of these lines are the two context interfaces and their docs, and about 90 are the `Coverage*` type declarations — the price of the seam being explicit rather than a closure. **M**, if the share-scope half (`collabMemberOf`, `shareLinkMemberOf`, `redeemShareLink` — roughly 150 lines) is worth its own file once something else needs `shareWorkspacesOf`: those three are the only members that are an access decision rather than a reading of the board, and they are already written as a pair of deliberately-different answers over one resolver. |
| `packages/server/src/request-admission.ts` | 673 | Exception | The request-admission run, out of the top of `fetch` in A17: may this request reach this server at all, and as whom. One file because the pieces are one decision read at three depths — the host gate classifies, the auth branch its decision selects proves an email, and the scoping turns that into a `visitor` — and each depth is only correct in terms of the one above it. Splitting the auth branches from the host gate would be the tempting cut and it is the wrong one: every branch re-checks that its own verifier exists, because "I could not verify" must never mean "serve it", and that rule is only legible beside the classification that chose the branch. The origin policy is here for the same reason one layer out: `isAllowedBrowserOrigin` and `corsHeadersFor` ask which surface this is, and answering it twice would let the CORS view and the host view of "our own machine" drift. About 190 lines are the two context interfaces, the union and their docs — the price of a per-request seam being explicit — and roughly 300 are the gate itself, which moved verbatim. **M**, if the origin policy (`createOriginPolicy`, `canonicalOrigin`, `applyCors` — about 90 lines) is worth its own file: it is already a separate factory, built at a separate point, because `policyFor` is an input to `createIdentitySetup` whose `accessOnlyBrowserHosts` is an input to the gate. That cycle through the identity setup is the reason it is two calls and not one, and it is also the argument for leaving both in one file until something else needs the policy. |
| `packages/server/src/shell-static.ts` | 627 | Exception | Shell and static serving, out of `createServer` in A18: which page or asset an address gets. One file because it is one decision read at two depths, and the tempting cut — helpers here, routes there — is what would break it. `isMockupDoc` exists because a mockup sent to the doc shell paints an empty page under a 200, which is the worst failure shape available since the status says it worked, and FOUR route blocks branch on it. `addressableWorkspaceFor` exists because a share visitor must be redirected to the workspace they were shared rather than whichever one holds the doc first — the bug where an old `/review/<docId>` bookmark 403'd for exactly the people shares exist to serve — and three blocks call it. Separated, both rules survive only as long as every future block remembers to ask, and neither failure is visible from the block that forgot. About 120 lines are the context interfaces and the module doc; the rest is the helpers and the run, moved with one substitution. It reads `visitor` and never re-decides on it: admission has already scoped the request, and the only uses here are choosing a redirect target and dropping the "all workspaces" arrow from a visitor's hub shell. **M**, if the three STATIC roots (`/widget/`, `/app/`, `/demos/` and the root-aliased assets — about 60 lines of `serveStaticUnder` calls) are worth separating from the rendered shells: they share no helper with the rest, and the argument against is that the containment checks over them are the file's security surface and are easier to audit in one place. |
| `packages/server/src/stall-wiring.ts` | 938 | Exception | The stall / ready-work wiring, out of `createServer` in A14. One file because it is one reading: every member answers "is this board moving, and who has to be told", and the two snapshots exist only to be handed to the two nudgers that share the tick. Splitting ready from stall is the obvious cut and it is the wrong one — they share `parallelismCapView` / `capSummary` (the cap must mean the same number in both wakes), the same `buildQueue` priority order asked with `includeBlocked`, and the same `sse.agentsOn` addressing rule, and the comment-queue bridge is what makes a comment count as movement for the stall clock at all. Two files would put the ready wake's reading of the cap in one and the stall wake's in another with nothing asserting they agree, which is the drift the shared `evaluateStalls` / `evaluateReadyWork` pair exists to close. About 180 of these lines are the context interface and its doc — the price of the seam being explicit rather than a closure. **M**, if the comment-queue bridge (`onDocRoomEvent`, `queueCommentRows`, `markCommentRowsEmitted`, `commentAuthorIs` — roughly 140 lines) is worth its own file: it is the one member that is a delivery path rather than a reading of the board, and it came along because the stall loop is its only other reader. |
| `packages/server/src/review-gate.ts` | 783 | Exception | The review-item quality gate, out of `createServer` in A12. One file because "judged, then announced, in that order" is the rule the whole family exists to keep: a declaration filed on a TICKET and one filed on a COMMENT land in the same reader's queue, so both run the same judge, and a push whose title is the item's headline must never go out for an item that queue omits. The two judges, the two hold-message spellings, the ask-back that turns a question into a thread and the re-judge after a ticket's words move are all written in terms of one another — `runReviewGate` is the shared body and everything else is a target for it. Splitting the ticket half from the comment half would put the ordering rule in two files with nothing asserting it, which is the drift the gate was built to close. `announceReviewItem` deliberately does NOT live here: it arrives in the context from `push-announce.ts` (133 lines), so a caller cannot announce without asking the gate first. |
| `packages/server/src/routes/docs.ts` | 729 | Exception | The `handleDocResourceRoutes` seam this row used to describe is done: the `/api/docs/:id/...` resource block split along `rest ===` families into `doc-resource.ts`, `doc-threads-routes.ts` and `doc-edit-routes.ts`, verified byte-identical and in-order the same way A1 verified the whole-file move. `docs.ts` still owns the `docId`/`room`/`rest` resolution those three delegate from, the two remaining entry points (`handleDocCreateListRoutes`, `handleDocPromoteRoute`, ~270 and ~130 lines — neither over the line on its own), the context types both halves of every split need (`DocRoutesContext`, `DocRouteRequest`, `DocResourceRouteRequest`, `ThreadReviewGate`), and the helpers shared by more than one of the three new files (`withSyncError`, `parsePlacement`, `parseSuggestionAuthor`). Just over the line on the shared-type weight alone. |
| `packages/server/src/routes/doc-threads-routes.ts` | 733 | Exception | Thread creation (`threads` POST, `threads/by_find` POST) and every per-thread mutation (`threads/:threadId/...` — reply, answer, revise, withdraw, resolve, reopen, reanchor, the anchored-edit trio), plus `reviewFromBody`, the declaration checker all three route groups call and nowhere else does. The ticket that produced this file filed the `threads/:threadId/...` block under `doc-resource.ts`'s `home` bucket; it moved here instead because every route in it is a thread mutation, not a doc-resource read, and splitting it from `threads` POST would separate two directions of the same write (create a thread vs. mutate one) plus the gate/dedup helpers both need. **M**, if the per-thread mutations (reply/answer/revise/withdraw/resolve/reopen/reanchor) are worth their own file from the anchored-edit trio (rewrite_region/insert_after/insert_blocks_after) and thread creation — the split point would be "read/react to a thread" vs. "restructure one," which is a real seam but wasn't this PR's job. |
| `packages/server/src/shells.ts` | 930 | Exception | One job: the HTML this server renders before any bundle runs. Every shell is a complete document with its own `<style>`, which is the point — nothing here may depend on an asset that has not loaded. Splitting per page would separate each shell from the `escape` / `landingShell` / `HTML_SHELL_HEADERS` trio all five share, and the file has no logic to test apart from the strings. |
| `packages/server/src/routes/auth-share.ts` | 781 | Exception | Two families that look separable and are not: the share routes' refusals are stated in terms of the session the auth routes mint, and the order between them is behaviour (the browser-write refusal sits above every share route so a mint added later is covered by construction). A split would need the boundary written down in two headers instead of one, and the chain position asserted twice. |
| `packages/server/src/routes/meetings-calendar.ts` | 562 | Exception | Just over the line, and one chain position: every `/api/docs/<id>/meetings...` pattern must be tried before the doc catch-all. Splitting meetings from calendar would make that one ordering constraint span two files for 62 lines of relief. |
| `packages/server/src/server-config.ts` | 514 | Exception | One job, and the reason it is one file is the job: turn the environment into typed config in ONE place, so nothing downstream reads `env` inline. It crossed the line when the access-only browser-host rule and the emailed-code sign-in switch arrived, each of which is a resolved value plus the boot warning that fires when the deployment cannot honour it. Splitting by subject would put a variable's default in one file and the warning that explains its absence in another, which is the reading this file exists to keep together. |
| `packages/server/src/workspace-store.ts` | 569 | Exception | The board registry, moved whole out of `tasks.ts` in A2 — create, rename, retire, delete, the lead seat and doc links, plus the pure `isRetired` / `retiredNotice` / `normalizeWorkspaceName` helpers that live here so `task-agents.ts` can import them without importing the file that imports it. Just over the line, and every verb reads or writes the same `WorkspaceState`; a split would put the retire refusal in one file and the delete that must honour it in another. |
| `packages/server/src/task-agents.ts` | 1197 | Exception | Agents on a board: attachments and the lead seat. The two delivery queues this row used to flag as the next seam — voice change-requests and comments — are now split out, into `agent-voice-queue.ts` (242 lines) and `agent-comment-queue.ts` (249 lines): `AgentStore` composes one instance of each and its own methods delegate to them, so its public API is unchanged. What is left is one subject — who is attached, how recently the server saw them, whether the lead seat is held by someone still there — and every read of it (`isDeliverable`, `leadSeatHealth`, `hasLiveAttachment`) shares the same observed-clock-plus-socket-probe logic; splitting it would put that logic in one file and the callers that depend on its exact meaning in another. |
| `packages/server/src/task-goals.ts` | 811 | Exception | The goal bands, moved whole out of `tasks.ts` in A2. It looks like two files — the band list (`setGoalList`, `renameGoal`, `addGoal`, `reorderGoals`) and placement (`setTaskGoal`) — and is deliberately one: placement IS triage on this board, so all five have to agree about what a band change does to the rows sitting in it, and `setGoalList` calls the same sweep `setTaskGoal` does. Splitting them would put that agreement in a comment instead of in one reading. |
| `packages/server/src/tasks.ts` | 3085 | Exception | The verb families the previous row named as the remaining seam are out: `task-authoring.ts` (461 — create, rename, body edits), `task-lifecycle.ts` (479 — the status gate, assignee, due date), `task-archive.ts` (356 — archive/unarchive, task and band), `task-links.ts` (354 — `after` edges and cross-references) and `task-notes.ts` (210 — notes, estimates, artifact checks, reading time), each behind its own narrow persistence interface, the same seam `ReviewItemStore` / `GoalStore` / `AgentStore` / `WorkspaceStore` already use. `openBlockers` and `announceUnblocked` are module functions in `task-lifecycle.ts` because the archive needs both and must derive them the same way a close does; the four pure predicates all five needed went to `task-helpers.ts` (177) and are re-exported here. What is left is not a fifth responsibility but three layers of one: the state (the four `Map`s, which `task-persistence.ts` writes through `TaskPersistenceHost`), the WIRE CONTRACT that state is stated in — roughly 1,600 lines of `…Event` and `…Result` types every module and route imports from this path — and one thin forwarder per verb onto the module that owns it. Splitting further would mean a types file, which would move each type away from the verb that documents it, or moving the forwarders, which is the compatibility surface itself. |
| `packages/server/src/rooms.ts` | 3035 | Split | A3 took document mutation out to `doc-edit-ops.ts`, threads to `doc-threads.ts` and the workspace surface to `rooms-workspaces.ts`; the bindings then came out to `file-binding.ts`, and the websocket fan-out this row named as the next seam is now `room-fanout.ts` (see below), behind the room-object handle it asked for. What is left is the room LIFECYCLE — `getOrCreate`, `hydrateDoc`, `evictIdleRooms`, `teardownRoom`, `flush`, the `.ydoc` persist, the alias claim and the doc index it writes — plus a delegating face over the five collaborators: some fifty one-line forwarders (`postComment`, `findAndReplace`, `attachFile`, `bindDiff`, `closeSocketsForShare`, …) that exist so no caller has to learn which collaborator now owns a verb. The next seam is inside the lifecycle rather than beside it: what this server can answer about a doc WITHOUT opening its room — `list` / `listFromIndex`, the `threadCounts` and `lastThreadActivity` pairs, `withActivity`, `getDoc` / `getDocStatus`, and the index and alias writers that keep those answers true (`indexEntryFor`, `indexUnindexedDocs`, `seedAliasesFromIndex`, `claimAlias`). That reads one map and one on-disk index and never touches `room.ydoc`, so unlike the fan-out it needs no room handle at all. **M** |
| `packages/server/src/room-fanout.ts` | 717 | Exception | Who hears about a change to a room and over which channel, out of `rooms.ts`: `wireEvents` and the two meta guards that ride it, `fireEvent` / `fireSuggestionEvent` / `scheduleSummary`, `broadcastToRoom`, the shared presence ticker, and the socket closes a revoked share or a teardown performs. One file because they are one frame's journey, not five subjects — `fireEvent` stamps `room.seq` and ends in `broadcastToRoom`, which stamps the one event id every channel must carry, and the share sweeps and the teardown close are the same `room.conns` walk under two close codes. Splitting them would put a frame's id in one file and the channels that must agree about it in another. Its handle is the ROOM OBJECT, not a docId: every path reads `room.ydoc`, `room.conns`, `room.meta` or `room.seq` on the frame it is building, so a lookup-by-id version would re-resolve the same room several times per broadcast and could disagree with its caller about which one it has. It reaches the lifecycle only through `RoomFanoutHost` — thunks onto the SSE hub and webhook dispatcher, the persist debounce, the companion/review projections and the resident room map, never a copy of any of them. |
| `packages/server/src/file-binding.ts` | 1652 | Exception | The file bindings, out of `rooms.ts`: `attachFile` and its two flat-text twins, the shared mtime sweep, the debounced write-back, the conflict reconcile that arbitrates when doc and disk both moved, and the doc-home pin. One file because it is one contract, not four subjects — every path is a rule about which side of a bound doc wins, and they are written in terms of each other. The write-back consumes a pending reconcile before it writes; the reconcile's conflict arm re-arms the write-back; the attach-time arbitration reaches the same backup-then-reassert branch a live conflict does; `homeGuard` is the gate on BOTH directions. Splitting them would put a decision in one file and the rule that decides it in another, which is exactly the drift that cost content twice (2026-08-03, 2026-09-01). It reaches the room lifecycle only through `FileBindingHost` — thunks onto the rooms map, the `.ydoc` persist debounce, the residency clock and the room's event fan-out, never a copy of any of them. |
| `packages/server/src/rooms-workspaces.ts` | 1040 | Exception | The workspace surface of a review, out of `rooms.ts` in A3: the projections that answer "what is in this review" (tree, grouped diff, all-files, thread roll-up, the lazy context and companion opens) and the archive verbs that retire one as a unit. Both walk the same member list — `list()` filtered by `reviewIdOf` — and the archive path is written in terms of the projections' own vocabulary, so splitting them would leave two files reading one index. |
| `packages/server/src/doc-threads.ts` | 755 | Exception | Comment threads, moved whole out of `rooms.ts` in A3: open, reply, resolve, reopen, re-anchor, and the review payload a thread can carry. The review verbs look like a second file and are not: a review IS a field on a thread here, all five end in the same `thread.replied` frame, and splitting them would put the payload's rules in one file and the thread they are a property of in another. |
| `packages/server/src/voice.ts` | 1318 | Exception | A6 took the classification to `voice-prompt.ts` and the write guardrail to `voice-action.ts`. What is left is `VoiceRouter` — the one thing here that is not pure. It holds the pending-choice map, the retry window and the completer, and every path through `handle()` ends in an ack plus a `voice.request` event, which is the promise this subsystem makes. Splitting the router further would separate a path from the ack it owes. |
| `packages/server/src/meeting-task-capture.ts` | 794 | Exception | A5 took the prompt and its reader to `meeting-capture-prompt.ts` and the transcript guards to `meeting-capture-guards.ts`. What is left is one pipeline: six intents parsed from one reply, each filed through the same board slice and accumulated into the same `CaptureLinks` the notes read back. The Haiku extractor at the bottom looks separable and is the consent seam this file's header is about — the transcript leaves the machine there, on the dedicated key, or capture stays off — so it belongs with the pass it feeds. |
| `packages/server/src/meeting-notes-merge.ts` | 758 | Exception | A5 took the ownership ledger to `notes-ownership.ts` and section reading to `notes-section.ts`. What is left is one algorithm in two halves that share a vocabulary: `planNotesMerge` decides which items are kept, inserted, suggested on or dropped, and `applyPlan` performs exactly that plan against the Yjs fragment. Splitting a plan from the only thing that applies it would put the invariant — an agent write touches only agent items — in one file and its enforcement in another. |
| `packages/server/src/deploy.ts` | 817 | Exception | A7 took the durable trace to `deploy-log.ts` (282, against an estimated ~250). What is left is one deploy: the decision (`decideDeploy` over `DeployFacts`), the executor (`runDeploy`, `launchctlRestart`, the git and `bun install` runners) and the single-flight `Deployer` that serialises them. The record's four types went WITH the trace and are imported back — `DeployResult` is a wire format between three processes that never meet, not an internal type of the runner. |
| `packages/server/src/meeting-notes.ts` | 870 | Exception | A5 took the two clocks to `pause-ticker.ts`. What is left is mostly this subsystem's contract surface — the turn, compose, relabel, correction and reattribution shapes every other meeting file imports — plus `beginNotesSession`, the session that produces them. The types are not a file of their own: each one is documented by the verb beside it, and a types file would move the documentation away from the behaviour it describes. |
| `packages/server/src/task-projection.ts` | 743 | Exception | A2 took `projectTask` out to `task-row.ts`; what is left is the room lifecycle — `ensureWorkspace`, `refresh`, `scheduleSnapshot` and the debounced snapshot writer — which is one object's behaviour over one `Rooms`. The owner readers (`ownerKindReader`, `claimSessionReader`) still read as a separable pair and would move beside `task-owner.ts`; that is ~90 lines and does not justify a third file on its own. |
| `packages/server/src/notes-section-write.ts` | 511 | Exception | Every write into a notes section, out of `meeting-notes-doc.ts` in A5: replacing the section, the research placeholder, and the three passes that rewrite a voice already written about (`retagSpeakerInNotes`, `reattributeNotesSection`, `relabelNotesSection`). Eleven lines over, and the tag machinery under those three is the seam — roughly 270 lines that only touch `Y.XmlText` runs and their marks. It stays for now because the three passes are ORDER-DEPENDENT with each other and with the section writers above them (the untagged sweep must run before the retag, or an extension rename writes the new name twice), and that ordering is documented at the call sites here. Splitting it is a diff about speaker tags, not about the line count that surfaced it. |
| `packages/server/src/meeting-notes-doc.ts` | 574 | Exception | A5 took every section writer to `notes-section-write.ts`. What is left is the sink: the per-doc ownership ledger, the context gather, and the tick handlers that decide WHICH write a tick makes. Just over the line, and all of it reads the same `NotesDocRooms` slice. |
| `packages/server/src/stall-escalation.ts` | 573 | Exception | One `StallEscalations`: the board's own review item, from deciding a row qualifies to filing, revising, moving and withdrawing it. About 200 of the lines are the header — the incident it answers, why the item hangs on a ticket at all, and the masking trade-off that choice creates — because every rule in the class is a consequence of one fact (an open item makes its own anchor `blocked-on-owner`, so the gate stops naming it) and a reader who does not have that fact will read the anchor re-read, the cooldown and the re-anchor as three unrelated hacks. The obvious seam is rendering (`buildStallEscalationReview`, `span`, `BUCKET_WORDS`, `label` — about 90 lines) and it is already a pure function exported for its own tests; splitting it would buy a file boundary between the words and the single caller that decides which rows are in them. **S**, if a second surface ever renders these rows. |
| `packages/server/src/stall-nudge.ts` | 867 | Exception | One `StallNudger`: the tick, the arming stamps, and addressing. The held-item nudge (`nudgeFilers`) reuses the same tick, `reachable`, and stamp file, so lifting it out would duplicate all three. |
| `packages/server/src/ready-nudge.ts` | 843 | Exception | One `ReadyWorkNudger`. Most of the file is the not-sending rules (`armed`, `NudgeTally`) that only make sense against the frame they suppress. |
| `packages/server/src/review-queue.ts` | 602 | Exception | A6 took the ask predicate to `ask-detection.ts` (249, against an estimated ~250). What is left is one queue in three shapes: `reviewThreadItems`, `taskReviewItems` and `reviewItemRows`, which is the whole queue in one order. They share the band rule, the thread source and `classifyActor`'s judgement about who counts as an agent — a second notion of that is the drift this file exists to avoid. |
| `packages/server/src/voice-resolve.ts` | 588 | Exception | A6 took the spoken brief to `voice-status.ts` (187, against an estimated ~190). What is left is one resolver: `tokenize` and the trigram similarity feed `rankTitles`, which feeds `resolveByTitle`, and the intent detectors (`navigationAsk`, `statusAsk`, `hubDestinationAsk`, `goalOrdinalAsk`, `parseOrdinal`, `pickByLabel`) all read the same stopword list and the same confidence floor. It now imports nothing at all. |
| `packages/server/src/middleware/host-guard.ts` | 756 | Exception | One question — may this request proceed on this host. `collabScope` deliberately answers through `shareScopeAllows` rather than a second allowlist, because the allowlist that drifts open is the breach; `classifyHost` and the scope check are two halves of one decision. |
| `packages/server/src/home-brief.ts` | 743 | Exception | One feature end to end: `HomeBriefStore` plus the pure pipeline `briefEvents` → `deterministicBrief` / `buildBriefPrompt` → `acceptBrief`. The network half already lives in `ThreadSummarizer`. |
| `packages/server/src/transcribe-assemblyai.ts` | 735 | Exception | One protocol client end to end: `streamingUrl` builds the v3 session and `createAssemblyAiEngine` speaks the Begin/Turn/Termination sequence; `expiryFrom` and `resolveAssemblyAiKey` serve only that socket. |
| `packages/server/src/meeting-protocol.ts` | 655 | Exception | One class, `MeetingRelay`. The `/audio/<docId>` socket *is* the meeting lifecycle, so open, frames, and every ending path have to sit together to end it exactly once. |
| `packages/server/src/recall.ts` | 616 | Exception | One vendor client: `recallConfigFromEnv`, `buildCreateBotBody`, `createRecallClient`. The callback-URL helpers exist only because Recall dials us back. It also hosts `clip`, the failed-response clipper all three HTTP clients in this family share — A7 put it here because `recall-calendar.ts` and `google-oauth.ts` both call it and neither may import the other. |
| `packages/server/src/client-release.ts` | 593 | Exception | One lifecycle for the numbered release directory: `publishClientRelease`, `currentClientRelease`, `readPublishLedger`, and `prepareClientRelease` — which is the decision the other three exist to make. |
| `packages/server/src/recall-meeting.ts` | 572 | Exception | One class, `RecallMeetingRelay`: invite, webhook status, and the dialled transcript socket are three channels of one bot lifecycle. |
| `packages/server/src/sse.ts` | 566 | Exception | One fan-out. `SseHub` holds `byDoc` and the bounded `replay`; `openSseStream` is the socket that consumes both, including the `replay.gap` path. |
| `packages/server/src/identities.ts` | 557 | Exception | One store — the `Identities` class over `identities.json` — plus its sanitizer and the single projection `userForIdentity`. |
| `packages/server/src/summarize.ts` | 549 | Exception | One outbound-API owner, `ThreadSummarizer`. `generate`, `generateHomeBrief` and `backfill` share the key resolution, `post`, and the debounce maps that stop three browsers paying three times. |
| `packages/server/src/meetings.ts` | 516 | Exception | One durable record: the append-only transcript and index paths, the `listMeetings` / `readTranscript` folds over them, and `MeetingStore` as the live handle on the same files. |

## packages/workspaces-app

| File | Lines | Verdict | Reason / seam |
|---|---|---|---|
| `packages/workspaces-app/src/styles.css` | 2763 | Exception | The chrome every page shares, and only that: design tokens, the top bar, voice, the pointer pill, the editor's prose rules, the comment pill, the threads pane, both composers, the toast, the connection and stale-client banners, the read-only bar and the first-arrival identity prompt. B2 took the board's half into `hub.css` and the sign-in page's into `signin.css` (12042 -> 6545); this split took the review editor's own surfaces into `doc.css` (6545 -> 2783). Cutting the remainder further would split rules that three pages all download from each other and buy no page a byte, because every one of these three pages loads all of it. A rule goes under the banner naming the surface it paints (`grep -n '^/\* =' packages/workspaces-app/src/styles.css`); the test is whether more than one page can reach it, which is why `.thread-line` stayed here, why `.signin-bar` had to come back in B2, and why the suggested-edit marks and the `.cw-suggest-chip` default live here rather than beside the redline chrome they were authored next to: the board mounts the same `createEditor` over a task body, and `SuggestInsert` / `SuggestDelete` / `SuggestionChips` are base-schema, so a task description can carry a pending proposal. |
| `packages/workspaces-app/src/doc.css` | 3986 | Exception | The review editor's whole stylesheet — main layout, the review-set sidebar, the workspace file tree, the format bar, the reassign menu, code blocks and tables, the meeting record chrome and live zone, the over-doc sheet, the full-screen thread view and long-thread modal, the code-review and diff-review surfaces, the diff nav and the inline thread cards. Lifted out of `styles.css` in source order and loaded AFTER it, which is the half of this that was a decision: those rules sat interleaved through the base, so the split reorders 72,575 pairs of rules. None of them can decide a value in this order (same specificity, importance, property, element and pseudo-element box); twenty can in the other, which is why `index.html` links the two in the order it does. Same argument as `hub.css` for not cutting it per surface: it would trade labelled regions for an import graph between stylesheets, and the editor loads all of it on every visit. Two grouped selectors — the mobile chip reveal and the print hide — are split across the two files rather than lifted whole, because `.cw-suggest-chip` is minted by a base extension and `.cw-del-chip` is not. The print hide keeps its declarations and position verbatim. The reveal no longer does: it became `body:not([data-cards="balloon"])` when the comment surface stopped being a width rule, and BOTH chips are revealed from this file so the board — which writes no `data-cards` and would otherwise match at every width — keeps computing `none`. `doc-css.test.ts` reads both sides of that. |
| `packages/workspaces-app/src/hub.css` | 5527 | Exception | The board's whole stylesheet, lifted out of `styles.css` in B2 in source order so no rule changed the cascade it lands in. It is one page, and the 28 `##### HUB · …` sub-banners are the split: a rule goes under the banner naming the surface it paints (`grep -n '##### HUB' packages/workspaces-app/src/hub.css`). Cutting it into files per surface would trade a labelled region for an import graph between stylesheets, and the board loads all of it on every visit either way. |
| `packages/workspaces-app/src/hub/hub-board-model.ts` | 1397 | Exception | One model, the board itself: what a row is (`HubTask`, `ownerKind`, `statusLabel`), which section it lands in (`taskVisible`, `boardSections`, `goalSection`), how much work a goal has left (`boardEffort`, `goalEffortLabel`) and where a drag drops it (`dropTarget`, `stepTarget`). Split out of `hub-model.ts` (B1). Cutting it further would separate a row's shape from the ordering rule that reads it, which is the pair `byBoardOrder` exists to keep in one place. |
| `packages/workspaces-app/src/hub/hub-review-model.ts` | 1262 | Exception | One queue end to end: what a review item is, the ranking that puts everything waiting on a person into one list (`reviewQueue`, `decisionQueue`, `humanBlockerRows`), the walkthrough that walks it (`advanceWalk`, `walkHandoff`), and the wording each row and card wears (`reviewHeadline`, `askedMeta`). Split out of `hub-model.ts` (B1). The ranking and the wording are asserted against each other — a row's title is derived from the same ask the rank reads — so a further cut would need both halves imported back. |
| `packages/workspaces-app/src/hub/hub-presence-model.ts` | 1019 | Exception | One strip of chrome fed by one clock: who is here (`presenceChips`, `leadSeatLabel`), what is running (`pluginDriftNotice`, `clientDriftNotice`, `uptimeSummary`), what has happened (`describeEvent`, `activityRows`) and where the hub is standing (`navFromPath`, `homeSinceLabel`). The remainder of `hub-model.ts` after B1's other two cuts. `timeAgo` dates a presence chip, a release and a trail row alike, so a further cut would put that one clock behind an import in three places. |
| `packages/workspaces-app/src/hub/hub-actions.ts` | 640 | Exception | Every REST write the board performs, bound once to an explicit `HubActionDeps` instead of captured from `main()`'s closure, plus `HubState` — the projection those writes mutate — and the three primitives every verb ends in (`send`, `fetchJson`, `showToast`). Split out of `hub-app.ts` (B1). Splitting the verbs from the state they write, or from the toast that reports a refusal, would put the blast radius back behind an import. |
| `packages/workspaces-app/src/hub/hub-app.ts` | 792 | Exception | The hub's entry, and now only that: `bootHub`'s environment, the identity and write-gate preamble, the address bar (`currentBoardLocation` / `syncBoardUrl` / `applyHistoryLocation` / `setNav`), and the ordered composition that ends in the first paint. B1 lifted the REST writes (`hub-actions.ts`), the queue's controller (`hub-review-controller.ts`) and the live path (`hub-live-wiring.ts`); A11 took the shell, the settings panel and the walkthrough; A13 took the render closure itself, one region per module — `hub-islands` (166), `hub-projection` (148, which also holds `initialHubState` — the projection's own starting value belongs beside the read that fills it), `hub-board-region` (301), `hub-queue-open` (155), `hub-home-region` (176), `hub-discussion` (155), `hub-detail-panel` (572), `hub-chrome-region` (153), `hub-data-loads` (199), `hub-load-report` (79), `hub-deep-links` (190) and `hub-voice` (94), plus `wireNavCollapse` into `hub-shell.ts` and `wireHubShortcuts` into `hub-shortcuts.ts`. Each takes `HubState` plus one named deps object, so a region can only reach what its object lists. What is left is over the limit because a boot sequence is a sequence: read the order of the `const`s in `bootHub` and you have read the page, and every remaining candidate — the address bar, the load-report stamps, the first paint — is a step in that order rather than a separable subject. **S**, and only if the address bar ever stops being the thing every region writes through. |
| `packages/workspaces-app/src/hub/hub-detail-panel.ts` | 572 | Exception | One overlay, two things it can be showing — the task panel and the goal panel, out of `hub-app.ts`'s render closure in A13. They are one module because they are one container: `renderDetail` is the single function that decides which is up (task wins when both ids are set), and the hand-off between them is what no separate pair of files could keep true — opening a goal over a ticket closes the task panel with `handOverEditor`, because ONE body editor is mounted at a time and the goal mounts its slot synchronously while the task's close lands a microtask later. `bodyEditor`, `syncReadTracker` and the four `let`s that tell a repaint from an open all exist to serve that one render, and `renderDetail` runs on every board event and clock tick, so each of them is the memory that makes "once per open" possible. `HubDetailDeps` is the longest deps object in the hub and that is the honest shape of the panel — it is where a ticket's every verb is reachable — but it is written out, so the panel cannot reach anything absent from it, which the closure it came from could. |
| `packages/workspaces-app/src/hub/hub-detail-render.ts` | 1145 | Exception | One panel: who has the task (`assigneePicker`), what it says (`detailFields`, `bodySlot`), what it cost (`effortFields`, `effortComputationLines`), what it points at (`renderTaskLinks`, `renderRelatedLinks`) and what happened to it (`activityRow`, `renderTransitionRow`). Split out of `hub-render.ts` (B1). Larger than the plan's ~900 estimate because the doc-title hydration helpers and `assigneePicker` came with it — both are used only by this panel, and leaving them behind would have pointed `hub-render.ts` back at this file. |
| `packages/workspaces-app/src/hub/hub-render.ts` | 770 | Exception | The hub's shell chrome after B1: the topbar, the lead-agent strip, the archived list, the goal detail panel, the quick actions, the review banner, the Home brief and the activity view. Eight small regions that share the shell's own idioms and nothing else; the three regions with a vocabulary of their own are now `hub-detail-render.ts`, `hub-discussion-render.ts` and `hub-review-render.ts`. |
| `packages/workspaces-app/src/meeting-strip.ts` | 1215 | Split | B4 took the DOM-free wire layer to `meeting-protocol.ts` (215) and the start chooser to `meeting-chooser.ts` (433); this pass took the two surfaces that answer to nothing but their own element — the transcript feed to `meeting-feed.ts` (281) and the speaker menu to `meeting-menu.ts` (192) — which is what took this file from 1476. Both left behind an explicit deps object of accessors read at call time, the `MeetingChooserDeps` shape, because every fact they render is a `let` the state machine moves under them. What is left is the socket state machine and the one closure it lives in. Measured: 363 lines before `mountMeetingStrip` are the module's header, `StripState`, and the 144-line `MeetingStripOpts` injection surface that lets every state below be driven without a server; the remaining 852 are the mount itself, of which `render` → `stop` (lines 770–1054) is the machine proper — `handle`'s switch, `start`'s capture-and-connect, `setState`, `closeSocket`. It does not reach 500 because the rest of the mount is not a set of jobs but one set of eighteen mount-level `let`s (`grep -c '^  let ' packages/workspaces-app/src/meeting-strip.ts`) — `state`, `socket`, `socketOpen`, `capture`, `turns`, `view`, `mode`, `generation`, `names`, `seen`, `lastMeetingId`, `disposed` and the rest — that `handle`, `start`, `stop`, the DOM the strip builds and the three deps objects all read and write. The next seam a reader will reach for is the strip's own render plus the button chrome it draws into (~200 lines with the element construction); it was not taken because its deps run to a dozen, which is the same closure passed as an argument and called a seam. **M** |
| `packages/workspaces-app/src/review-chrome.ts` | 824 | Split | B3 took the two writing surfaces to `doc/review-composer.ts`, the panel resize handle and thread-range clicks to `doc/chrome-panels.ts`, and the shared DOM helpers to `doc/chrome-dom.ts`. The seam this row named — the thread projection — is now `doc/thread-projection.ts` (263): `collect`, `resolveRange`, `refreshDecorations`, `lineLabel` and the three pieces of state only they touch (the active thread id, the pending-summary expiry timer, the seen tracker's in-place dot removal), with the ydoc, the surface and the tracker passed in and one `onPendingExpiry` callback back. The same pass took the per-device view preferences to `doc/view-prefs.ts` (122) and the five server writes a card can make to `doc/thread-actions.ts` (156), and a review round then took `ChromeSelection` + `anchorBody` to `doc/anchor-body.ts` (44) — a leaf, because leaving the anchor build here made both of those modules import a VALUE back out of their own parent. What remains is `mountReviewChrome`'s wiring: the drawer and its tabs, the panel and the wide modal and the routing between them (`engageThread`, `maybeOpenModal`, `revealThread`), the mobile mount, the composer handoff, the doc label, the ydoc observer, the hotkeys and `destroy`. The next clean cut is the doc-title label (`renderDocLabel`, `docLabel`, `mobileLabel`, ~70 lines) — the one remaining piece that reads nothing the mount builds. The engagement router is the seam a reader will reach for first and is NOT it: it reads four of the five handles the mount constructs (`threadsPanel`, `threadModal`, `mobile`, `surface`), so lifting it would mean passing the mount back in. **M** |
| `packages/workspaces-app/src/doc/thread-card.ts` | 812 | Exception | One comment thread as a card, out of `threads.ts` in B3 — and the same card the redline margin renders as a balloon, which is why it is shared rather than duplicated. It looks like two files, the card and the eight DOM helpers under it, and is deliberately one: every helper has this card as its only caller, and the four faces a card can show (summary, detail, collapsed, answered) are built together because the fold cross-fades between faces that must already exist in the same box. |
| `packages/workspaces-app/src/thread-modal.ts` | 561 | Exception | One dialog and the journey it takes to get there: `mountThreadModal`, the promoted card it paints, and the promote itself — the box growing out of the margin bubble the reader tapped and shrinking back into it. Crossed the line adding that promote. The tempting seam is the animation, and it is not one: the promote writes the resting box, cancels the journey a switch replaced, and calls the morph on the card `paint` built, so lifting it out would hand a new module the dialog's root, its inner wrapper, its scrim, the open thread id and both in-flight handles — the whole closure passed as an argument. |
| `packages/workspaces-app/src/comment-hints.ts` | 510 | Exception | One answer to "what am I not seeing?", in three controls fed by one measurement: the two off-screen hints (`splitOffscreen`, `renderHint`), the top-bar "N waiting on you" chip and the walk behind it (`stepAsks`, `openAsks`), and the dwell timers that decide when a thread stops being new. Crossed the line when the chip learned to step. All three read the same `items()` pass over the thread rects, and the pure half a reader would cut first — `splitOffscreen`, `hintParts`, `chipSentence` — is already exported and tested apart from the mount. |
| `packages/workspaces-app/src/redline/markup-margin.ts` | 718 | Exception | B3 took the card builders to `balloon-cards.ts` and the phone sheets to `margin-sheets.ts`. What is left is the margin's own geometry and the render list it measures: `positionBalloons` reads four elements' rects plus the private rendered-balloon union, and `relayout` is the orchestration point that calls the deletion grouper, the thread filter and the renderer in order. The plan had both joining `balloon-layout.ts`; they did not, because that file holds the pure stacking algorithm and taking the glue there would point the dependency backwards — see split-plan.md B3. |
| `packages/workspaces-app/src/voice-capture.ts` | 705 | Exception | One gesture controller: `createVoiceCapture` and the hold rules (`SPACE_HOLD_ARM_MS`, `spaceHoldTargetsPage`) plus the messages that hold produces. All of it is the hold-to-talk surface. |
| `packages/workspaces-app/src/diff-nav.ts` | 598 | Exception | One sidebar renderer with two views sharing the toggle, `viewKey` and `diffNavSignature`. The all-files half is ~110 lines and cannot be understood apart from the toggle that selects it. |
| `packages/workspaces-app/src/meeting-advanced.ts` | 581 | Exception | One panel. The `AdvancedControl` descriptors exist only to drive `buildAdvancedSection`, and the canonical specs already live in core's `meeting-tuning.ts`. |
| `packages/workspaces-app/src/code/code-editor.ts` | 550 | Exception | One CodeMirror surface: `createCodeEditor` returning `CodeSurface`, with the gutter and decoration internals that only that editor instantiates. |
| `packages/workspaces-app/src/suggest-input.ts` | 510 | Exception | One ProseMirror plugin. `SuggestInput` and the handlers it installs all mutate the same `suggestInputKey` state. |

Every workspaces-app seam above moves symbols within one entry's import graph, so
the shipped bundle is unchanged.

## packages/core

| File | Lines | Verdict | Reason / seam |
|---|---|---|---|
| `packages/core/src/prose-markdown.ts` | 1183 | Exception | Markdown in and markdown out, out of `prose.ts` in B5, and deliberately one file rather than a parser and a serializer. They are one grammar: a mark the parser learns and the serializer does not emit is a doc that loses text on its next flush, so the two halves of that invariant must be readable together. `applyMarkdownToFragment` is the pair composed under an LCS diff, which is what makes a reparse touch only the blocks that changed. |
| `packages/core/src/review-item.ts` | 766 | Split | The seam this row named has been taken: the twelve contract types are now `review-item-types.ts` (404, no row needed), which emits nothing, so `review-item-wire.ts` (338) reaches them without importing the verbs back. What is left is the payload lifecycle the earlier splits left behind — `applyReviewRevision`, `withdrawReview` / `reinstateReview`, `answerFromReply` and `pendingDeclaration`, `reviewItemState` and the small derivations under it — plus the facade that re-exports all three siblings under the names callers already import. The next seam is the quality-gate predicates, `isReviewItemHeld` / `isReviewItemGated` / `isReviewPayloadHeld` / `isReviewPayloadGated` / `reviewPayloadVersion` and the two `judgeReason*` renderers: ~120 lines that read a verdict nothing else here writes. **S** |
| `packages/core/src/prose-edit.ts` | 857 | Exception | The two ways an agent edits prose without a cursor, out of `prose.ts` in B5: by what the text says (`locateMatches`, `findAndReplace`) and by a stored pair of relative positions (`rewriteRange`, `insertAfterRange`). One subject, not two — they share the covering-mark machinery, the near-miss hints a failed find returns, and the table-row path, and every one of them wraps its mutation in the same single transaction. |
| `packages/core/src/prose-blocks.ts` | 712 | Exception | Whole-block edits and the agent anchors that name where, out of `prose.ts` in B5. Everything else in that family edits text inside a block; this is the file that changes the document's shape. Anchors ship with block deletion because they are one contract read from two ends — an anchor promises a place can be found again, deleting the block it names is the edit that breaks the promise, and `autoReanchorDoc` is what runs afterwards. |
| `packages/core/src/task-wire.ts` | 759 | Exception | One wire contract, all data: `Task` alone is ~415 documented lines, plus `TASK_STATUSES`, `Ref`, `ArtifactCheck` and the single `byBoardOrder`. Splitting it would put one row's shape in two files, which is the drift the file exists to prevent. |
| `packages/core/src/speaker-tags.ts` | 619 | Exception | One tag format end to end: `speakerTagHref` and `parseSpeakerTagHref` define it, and `findSpeakerTags` / `normalizeSpeakerTags` / `reattributeSpeakerTags` are the operations on it over a shared `SpeakerTagMatch`. |
| `packages/core/src/suggest-ops.ts` | 572 | Exception | One registry and its mutations over a Y.Doc: `scanSuggestions` builds the read model that `acceptSuggestion` / `rejectSuggestion` / `resolveAllSuggestions` then act on, all through the shared `resolveOne`. |
| `packages/core/src/types.ts` | 558 | Exception | One cohesive type registry — `DocType`, `DocMeta`, the `Anchor` union, `Thread` / `Comment`, `WebhookPayload` — with a single function, `contentKind`, which is the table the rest branch on. |

## packages/mcp

| File | Lines | Verdict | Reason / seam |
|---|---|---|---|
| `packages/mcp/src/tools/workspace.ts` | 528 | Exception | The third of the same shape: one dispatch family — the arms addressed to a board rather than to a document or a row — over one shared context, plus `setBoardRetired`, which two of them are. Length is a count of tools, not of coupling. |
| `packages/mcp/src/tools/tasks.ts` | 1070 | Exception | The other half of the same shape as `tools/docs.ts` below: one dispatch family — every arm that addresses a task, by `taskId` or by a `reviewItemId` that resolves to one — over one shared context. The four helpers with it (`TaskPayload`, `taskCreatedSummary`, `heldResult`, and the two review-item lookups) have no reader anywhere else, which is why they are here and why moving them out would only add a file. Length is a count of tools, not of coupling. |
| `packages/mcp/src/tools/docs.ts` | 819 | Exception | One dispatch family: every arm that names a `docId`, over one shared context, in the order they stood in the switch. Cutting further would put one `switch` across two files and re-open the question the domain split just answered — which file a tool is in — for no reader's benefit, since nobody reads an arm they did not come looking for by name. The arms are independent by construction, so length here is a count of tools, not of coupling. |
| `packages/mcp/src/tool-schemas.ts` | 2012 | Exception | Declarative data, not code: the `tools/list` result — 94 tool schemas, names, descriptions and JSON Schema, with no logic and no branch in the file. The split that created it is what the `mcp.ts` row above asked for, and B6 accepted the size in advance: one table read top to bottom is what an agent picks a tool from, and cutting it into three would mean choosing which third a reader looks in. Splitting is possible the day a subset stops being read as one list. |
| `packages/mcp/src/nudge-line.ts` | 510 | Exception | One responsibility, the wording of the hub's wake events. `readyIdleLine`, `stalledLine`, `reviewItemHeldLine` and `reviewAnsweredLine` share `truncate` and `humanDuration`, and the point is that the wording is asserted in one place. |

## packages/widget

| File | Lines | Verdict | Reason / seam |
|---|---|---|---|
| `packages/widget/src/widget.ts` | 598 | Exception | What is left of the custom element after B7 took the three method clusters out: `widget-auth.ts` (284), `widget-picker.ts` (232) and `widget-threads.ts` (297). The remainder is one object — the element's own lifecycle. Construction, `init`, the attribute upgrade, the context it keeps in step with `location`, the WebSocket, the shadow-DOM shell, the three posting verbs the other modules reach through, and the observer that schedules a render. Every one of those reads the element's fields directly, and a further split would trade them for an interface. |

## packages/plugin

No file in this package is over the limit. `hooks/lib/agent-notes.ts` was,
until B8 took the reduction out to `note-redact.ts` (430) and left the hook
plumbing at 241.

---

## Test files

95 test files exceed 500 lines. Two hold two unrelated harnesses; the other 93
are exceptions, listed after them. The recurring shape across the exceptions is
one feature tested at two or three *layers* — pure predicate, then store, then
real HTTP route — sharing the same fixture builders. That is one harness.

| File | Lines | Verdict | Reason / seam |
|---|---|---|---|
| `packages/workspaces-app/test/hub-render.test.ts` | 3882 | Exception | Twelve describes over one harness: the module-scope `task()` factory and the `root` beforeEach, asserting rendered DOM. The six that `readFileSync` `styles.css` and hub source and assert on text moved to `hub-source-contract.test.ts` in B1 — they were the second harness this row named. |

The remaining 93 are exceptions. Each row names the one harness its cases share.

| File | Lines | Reason |
|---|---|---|
| `packages/workspaces-app/test/hub-model.test.ts` | 2331 | All describes are pure model functions fed by the one module-scope `task()` factory. |
| `packages/workspaces-app/test/meeting-strip.test.ts` | 2102 | The four parser describes are the strip's own helpers; the other 18 go through `mount()` with `FakeSocket` / `FakeBot`. |
| `packages/server/test/meeting-notes.test.ts` | 2045 | Every describe drives the notes pipeline off the module-scope `ManualScheduler`. |
| `packages/workspaces-app/test/markup-margin.test.ts` | 1670 | All 17 describes mount through `mountSurface` / `mountMargin` and share the `afterEach` teardown list. |
| `packages/core/test/prose.test.ts` | 1645 | Every describe seeds a `Y.XmlFragment` via `seedDoc()` and its two anchor helpers. |
| `packages/workspaces-app/test/review-walkthrough.test.ts` | 1544 | All describes build fixtures with `task()` / `threadItem()` and render through `renderHomeReview` against the shared `root`. |
| `packages/server/test/review-item-gate.test.ts` | 1480 | A single top-level describe over one real server and the SSE `listenFrames` harness. |
| `packages/core/src/review-item.test.ts` | 1399 | All 22 describes exercise the review-item payload vocabulary off the `decision()` / `review()` builders. |
| `packages/server/test/review-queue.test.ts` | 1377 | Every describe feeds `comment()` / `thread()` fixtures into the queue functions. |
| `packages/server/test/meeting-task-capture.test.ts` | 1376 | All describes share `boardStub()` / `extractorOf()` and drive one capture pipeline. |
| `packages/core/src/goal-effort.test.ts` | 1322 | Every describe builds effort inputs with `ok()` / `task()` / `closed()` / `identity()`. |
| `packages/server/test/host-guard.test.ts` | 1301 | 16 describes, all pure predicates over the one module-scope `LOCAL` host fixture. |
| `packages/server/test/task-routes.test.ts` | 1185 | One top-level describe; every nested suite uses the same `beforeAll` server and tmpdir. |
| `packages/server/test/meeting-notes-doc.test.ts` | 1149 | All describes build a Y.Doc via `docFrom()` and read it back with `markdownOf()`. |
| `packages/server/test/host-scope.test.ts` | 992 | Both describes stand up the real route table over `makeMockCfApi()` — one guard, two share modes. |
| `packages/server/test/server.test.ts` | 990 | Each describe builds the same tmpdir and `createServer` handle; the `decideReconcile` suite is a helper of that code path. |
| `packages/server/test/voice.test.ts` | 960 | A single describe over one real server with an injected `complete`. |
| `packages/server/test/workspace-lead.test.ts` | 947 | Every describe constructs a `TaskStore`, or a server over one, for the same lead-seat contract. |
| `packages/server/test/review-item-routes.test.ts` | 943 | The module-scope `handle` / `base` server plus `mkdoc()` / `seedThread()` feed all nine describes. |
| `packages/workspaces-app/test/voice-capture.test.ts` | 939 | The four pure describes are helpers of the same capture module; the rest share `FakeRecognition` and `holdSpace()`. |
| `packages/server/test/ready-nudge-routes.test.ts` | 932 | A single describe over one real board, stream, and the `listenFrames` / `waitForFrames` harness. |
| `packages/server/test/ready-nudge.test.ts` | 917 | All describes run the nudger through the module-scope `board()` and `harness()` fake world. |
| `packages/server/test/task-tool-routes.test.ts` | 899 | One describe, one `beforeAll` server, four routes. |
| `packages/server/test/stall-escalation.test.ts` | 516 | Four describes over one subject: the filer against a real `TaskStore`, the told-time seam it reads driven through `StallNudger`, the loop isolation, and the words a reader sees. They share `board()`, `row()`, `toldMap()` and `queued()` — the last is the reason they stay together, since half these tests are about the difference between an item being open and the reader's queue actually showing it. |
| `packages/server/test/stall-nudge.test.ts` | 890 | All describes drive the stall pass through `board()` and `harness()`. |
| `packages/server/test/transcribe-assemblyai.test.ts` | 880 | Every describe runs the mapping over the module-scope `FakeSocket` and `harness()`. |
| `packages/server/test/mcp-durable-watches.test.ts` | 856 | All three describes spawn the shipped MCP bundle through the `McpChild` class against a real server. |
| `packages/server/test/review-item-comments.test.ts` | 847 | A single describe over one real server. |
| `packages/server/test/attachments.test.ts` | 831 | Pure, store and route describes all exercise one `AgentAttachment` record shape with shared `readAudit` / `listen` helpers. |
| `packages/server/test/deploy-runner.test.ts` | 790 | Every describe builds deps via `fakeGit()` / `deps()` / `fakeWait()`. |
| `packages/server/test/meeting-notes-merge.test.ts` | 789 | All describes build a doc with `docFrom()` and edit it with the shared `typeBullet` / `editBullet` helpers. |
| `packages/workspaces-app/test/thread-card.test.ts` | 784 | Nine describes, all through `mountPanel()` and `makeThread()`. |
| `packages/server/test/hub-share.test.ts` | 778 | A single describe over one server with `connectDoc` / `readSseUntil` helpers. |
| `packages/server/test/summarize.test.ts` | 776 | Every describe injects `fakeFetch()` into a `ThreadSummarizer` over `thread()` fixtures. |
| `packages/mcp/test/review-item-tools.test.ts` | 774 | All describes speak JSON-RPC to the one spawned bundle child via `call()` / `last()`. |
| `packages/workspaces-app/test/thread-morph.test.ts` | 752 | Every describe mounts via `mountCard()` and inspects `recordAnimations()`. |
| `packages/server/test/bind-diff.test.ts` | 744 | Both describes build the same `makeFixtureRepo()` git fixture; `makeRooms` extends it rather than replacing it. |
| `packages/server/test/goal-ids.test.ts` | 739 | Every describe seeds a `TaskStore`, or a server over one, for the same goal-id contract. |
| `packages/server/test/meeting-socket.test.ts` | 717 | Seven describes, each a real server plus the shared `AudioClient` class, varying only engine config. |
| `packages/server/test/access-share-scope.test.ts` | 708 | One describe, one `beforeAll` server, nested suites over the one Access-mode share the shared harness mints. Converted from `link-share.test.ts` when link mode was retired. |
| `packages/server/test/goal-reorder.test.ts` | 692 | Store and route describes both seed goals with `bands()` / `seededGoalList()`. |
| `packages/server/test/stall-nudge-routes.test.ts` | 689 | A single describe over one real board plus `listenFrames` / `waitForFrames`. |
| `packages/server/test/home-brief.test.ts` | 687 | Every describe feeds `ev()` / `input()` rows into the brief pipeline, store included. |
| `packages/server/test/meeting-ask-cues.test.ts` | 662 | The detector describes and the `parseTaskCaptureReply` describes are the two halves of one convention, and the second reads the first: every licensing case is built from the same phrase tables the first half asserts, over the shared `candidates` / `reply()` fixture. |
| `packages/server/test/sse-replay.test.ts` | 660 | Every describe exercises an `SseHub` stream through the shared `listenFrames()` and `settle()`. |
| `packages/server/test/refresh-workspace.test.ts` | 658 | All three describes build a `Rooms` through the module-scope `makeRooms(dataDir)` and `git()` helpers. |
| `packages/server/test/auth-write-gate.test.ts` | 656 | Every case, HTTP and y-sync socket alike, boots through the one `boot(requireSignInToWrite)` harness. |
| `packages/server/test/voice-hardening.test.ts` | 646 | The prompt, resolver and handle describes share the file's `PERSON` / `AGENT` / `INJECTION` fixtures with the one end-to-end describe. |
| `packages/server/test/decision-routes.test.ts` | 636 | One describe with one `beforeAll` server; every assertion reads an effect back over the same REST base. |
| `packages/workspaces-app/test/hub-detail-css.test.ts` | 632 | Every describe parses the same `CSS` string through the shared `rule()` / `media()` helpers. |
| `packages/server/test/task-import.test.ts` | 629 | The parser describes and the route describe both exercise `parseTrackerMarkdown` on the same synthetic tracker fixture. |
| `packages/server/test/goal-rename.test.ts` | 627 | Store and route describes share the `bands()` / `boardFor()` goal-id fixture builders. |
| `packages/server/test/sentry-server.test.ts` | 617 | All describes drive `src/sentry.ts` against the module-scope `startCaptureServer()` harness. |
| `packages/server/test/agent-watches.test.ts` | 615 | Store and route describes share the `AgentWatches` data-dir fixture. |
| `packages/server/test/comment-durability.test.ts` | 610 | Both describes use the shared `listenFrames()` / `settle()` SSE harness against the agent comment queue. |
| `packages/server/test/share-link-flow.test.ts` | 606 | Eight describes over one server: the share hostname, the owner hostname, two boards and a doc on each. The two-audience fixture is the point — every cross-check case is vacuous without both, so splitting the file would mean building it twice. |
| `packages/workspaces-app/test/plan-gate.test.ts` | 605 | One describe on `mountPlanGate` over the shared `root` / `stubFetch` / `stubTimers` seam. |
| `packages/server/test/parallelism-cap.test.ts` | 601 | Both describes assert the same workspace cap through the shared frame harness. |
| `packages/server/test/owner-kind.test.ts` | 599 | The pure `resolveOwnerKind` tables and the HTTP describes are two declared layers of one decision over shared fixtures. |
| `packages/core/test/thread-summary.test.ts` | 599 | Every describe builds its subject with the module-scope `makeThread()` ydoc builder. |
| `packages/server/test/meeting-e2e.test.ts` | 588 | One describe over the `ManualScheduler` / `AudioClient` / `waitFor` harness. |
| `packages/server/test/doc-eviction.test.ts` | 587 | Both describes drive the same injected-clock Rooms eviction fixture and the shared `onDisk()` reader. |
| `packages/server/test/member-boundary-lows.test.ts` | 765 | Six describes over ONE Access-fronted server, two boards, a doc, a thread and a row on each — the fixture IS the subject (a member of board A doing things about board B), and a second copy of it in a second file would be the thing most likely to drift out of agreement with the first. |
| `packages/server/test/collab-host.test.ts` | 582 | All three describes depend on the module-scope `jwks` / `signJwt` `beforeAll` and boot the same server shape. |
| `packages/server/test/task-review-queue.test.ts` | 581 | Module-scope `handle` / `base` plus `seedWorkspace` / `seedDecision` feed every describe. |
| `packages/server/test/task-review-item-routes.test.ts` | 579 | A single describe and one `beforeAll` server; every case is a write route read back over HTTP. |
| `packages/core/test/summary-generated.test.ts` | 578 | All describes are pure summary-module functions over the shared `thread()` builder. |
| `packages/server/test/agent-coverage.test.ts` | 573 | One top-level describe around the watch-coverage route; the nested block reuses the same server fixture. |
| `packages/server/test/meetings.test.ts` | 571 | Four describes, each constructing the same `MeetingStore`-on-disk fixture in an identical `beforeAll`. |
| `packages/core/test/suggest-ops.test.ts` | 571 | Every describe builds a Y.Doc with `docFrom()` and reads it back with `serialize()`. |
| `packages/server/test/recall-meeting.test.ts` | 559 | One describe over the `FakeRecall` / `ManualScheduler` / `transcriptFrame()` vendor harness. |
| `packages/mcp/test/nudge-line.test.ts` | 559 | All describes call the pure `*Line()` renderers with inline row literals; no setup exists to be disjoint. |
| `packages/widget/test/widget-auth.test.ts` | 550 | Every describe runs against the module-scope `stubGlobals()` / `importWidget()` widget harness. |
| `packages/workspaces-app/test/thread-modal.test.ts` | 549 | All describes mount through the shared `mount()` harness and its `comment()` / `thread()` builders. |
| `packages/server/test/proxied-trusted-host.test.ts` | 547 | All describes depend on the module-scope `jwks` / `signJwt` `beforeAll` and the shared `get()` helper. |
| `packages/server/test/task-review-items.test.ts` | 545 | A single describe over one `TaskStore` fixture and the shared `payload()` builder. |
| `packages/server/test/grouping-share-removed.test.ts` | 545 | One describe using the shared `makeMockCfApi()` Cloudflare stub and one server. |
| `packages/server/test/meeting-notes-correction.test.ts` | 543 | Predicate describes and the real notes-doc describes all build their subject with `docFrom()` / `NOTES()`. |
| `packages/server/test/recall-callback-gate-http.test.ts` | 542 | Every describe uses the module-scope `spinUp()` / `callback()` / `signBody()` two-hostname harness. |
| `packages/workspaces-app/test/md-composer.test.ts` | 542 | Every describe drives the one composer through the module-scope `mount()` / `surfaceOf()` helpers; the stylesheet describes now install the page's sheets over that same mount rather than grepping `styles.css`, which is what took it over the line. |
| `packages/workspaces-app/test/hub-mic-dock-css.test.ts` | 540 | One subject — where the mic sits at each tier — over one `installSheets` / `setViewport` harness. It crossed the line converting text reads into computed reads: a rule that used to be one `toMatch` is now a mount, a viewport, and a comment naming the browser check that owns the half happy-dom cannot see. |
| `packages/server/test/stall-gate.test.ts` | 538 | Every describe calls the module-scope `evaluate()` over rows from `task()`; no server anywhere. |
| `packages/workspaces-app/test/goal-actions.test.ts` | 537 | Pure model describes and DOM-panel describes share `task()` / `handlers()` / `sectionOf()` for one feature. |
| `packages/workspaces-app/test/activity-model.test.ts` | 536 | All describes are pure Home-activity functions over the shared `task()` / `note()` / `groups()` builders. |
| `packages/server/test/share-grouping-scope.test.ts` | 533 | One describe over the shared `connectDoc()` / `git()` fixture and a single `beforeAll` server. |
| `packages/workspaces-app/test/thread-modal-chrome.test.ts` | 529 | Every describe mounts the review chrome via `harness()` on the same `mountChromeDom()` / `fakeSurface()` base. |
| `packages/server/test/voice-actions.test.ts` | 525 | One describe, one `beforeAll` server; all cases route spoken verbs through it. |
| `packages/server/test/projection.test.ts` | 525 | One describe over the shared `connectDoc()` / `listen()` / `auditLines()` Yjs-client harness. |
| `packages/server/test/artifact-check.test.ts` | 522 | Classifier, store and server-wiring describes are the layers of one check and share `prRef` / `fetchStub`. |
| `packages/server/test/git-ops-vs-bound.test.ts` | 521 | Both describes build a real git repo with the shared `git()` / `cleanEnv()` helpers; `classifyExternalContent` is the bound-doc path's own classifier, not a foreign subsystem. |
| `packages/server/test/tasks.test.ts` | 519 | `TaskStore` and the small `Ref` describe share the one store and dataDir fixture. |
| `packages/server/test/effort-estimate-gate.test.ts` | 519 | A single describe with one stub-estimator server fixture. |
| `packages/server/test/cross-origin-routes.test.ts` | 517 | Both describes stand up a real server and probe it from the module-scope `EVIL` origin for the `CANARY` body; the trusted host and the public share host are two modes of one guard, not two harnesses. |
| `packages/server/test/sync-clobber.test.ts` | 517 | Both describes exercise the same write-back clobber path over the shared `makeRooms()` / `writeExternal()` fixtures, one directly and one over HTTP. |
| `packages/workspaces-app/test/mobile-review.test.ts` | 512 | Every describe uses the shared `harness()` mount plus `comment()` / `thread()` / `orphanThread()`. |
| `packages/server/test/room-fanout.test.ts` | 508 | Five describes over one fixture pair — `makeRoom()` and the recording `makeHost()`. Broadcast, thread frames, socket sweeps, presence and `wireEvents` are the stages of one frame's journey through the same fan-out object, and every case is built from those two helpers. |
| `packages/server/test/home-routes.test.ts` | 506 | All four describes construct their server via the one `makeHarness(summarizer?)` factory. |
| `packages/workspaces-app/test/write-gate.test.ts` | 503 | Every describe uses the module-scope `beforeEach` DOM reset with `docShell()` / `hubShell()`. |
| `packages/server/test/dispatch-routes.test.ts` | 502 | One describe over the shared `listenFrames` / `waitForFrames` fake-watcher server harness. |

---

## Split queue

Ordered by how much a split reduces the chance that an ordinary PR lands in a
god file: commits over the last 90 days first, size second. Churn is the column
that matters — `recall-calendar.ts` has a clean seam and one commit in three
months, so splitting it buys almost nothing.

| # | File | Lines | Commits (90d) | Size |
|---|---|---|---|---|
| 1 | `packages/server/src/server.ts` (split in A1 and A12) | 7131 | 233 | M |
| 2 | `styles.css` + `hub.css` + `signin.css` (split in B2) | 12042 | 158 | M |
| 3 | `mcp.ts` + `tool-schemas.ts` + `tools/` + the eight modules lifted for coverage (all under `packages/mcp/src`, split in B6 and again for coverage) | 5563 | 156 | done |
| 4 | `hub-app.ts` + the fifteen modules split out of it (A11 and A13) | 3880 | 102 | S |
| 5 | `packages/workspaces-app/src/hub/hub-render.ts` | 2707 | 95 | M |
| 6 | `hub-board-model.ts` + `hub-review-model.ts` + `hub-presence-model.ts` (was `hub-model.ts`, split in B1) | 3645 | 89 | M |
| 7 | `tasks.ts` + `task-authoring.ts` + `task-lifecycle.ts` + `task-archive.ts` + `task-links.ts` + `task-notes.ts` (split from `tasks.ts`) | 4945 | 87 | done |
| 8 | `rooms.ts` + `file-binding.ts` + `room-fanout.ts` (split in A10, fan-out lifted after) | 4801 | 71 | M |
| 9 | `app.ts` + the nine phase modules under `src/doc/` (split for the markdown mount) | 1959 | 55 | done |
| 10 | `packages/server/src/task-projection.ts` | 743 | 43 | done |
| 11 | `review-chrome.ts` + `doc/thread-projection.ts` + `doc/thread-actions.ts` + `doc/view-prefs.ts` + `doc/anchor-body.ts` (was `review-chrome.ts`, split in B3 and again on 2026-09-03) | 1409 | 24 | M |
| 12 | `thread-card.ts` + `threads.ts` (was `threads.ts`, split in B3) | 1230 | 20 | done |
| 13 | `meeting-protocol.ts` + `meeting-chooser.ts` + `meeting-feed.ts` + `meeting-menu.ts` + `meeting-strip.ts` (was `meeting-strip.ts`, split in B4 and again after it) | 1953 | 18 | done |
| 14 | `review-item-check.ts` + `review-item-wire.ts` + `review-item-types.ts` + `review-item.ts` (split in B5, contract lifted after) | 1979 | 18 | done |
| 15 | `prose-fragment.ts` + `prose-markdown.ts` + `prose-edit.ts` + `prose-blocks.ts` + `prose.ts` (was `prose.ts`, split in B5) | 2847 | 16 | done |
| 16 | `review-queue.ts` + `ask-detection.ts` (split in A6) | 831 | 15 | done |
| 17 | `widget.ts` + `widget-auth.ts` + `widget-picker.ts` + `widget-threads.ts` (split in B7) | 1411 | 13 | done |
| 18 | `packages/server/src/meeting-notes.ts` + `pause-ticker.ts` (split in A5) | 1039 | 12 | done |
| 19 | `voice.ts` + `voice-prompt.ts` + `voice-action.ts` (split in A6) | 2109 | 11 | done |
| 20 | `packages/server/src/meeting-notes-doc.ts` + `notes-section-write.ts` (split in A5) | 986 | 11 | done |
| 21 | `activity.ts` + `actor-identity.ts` (split in A7) | 582 | 11 | done |
| 22 | `packages/workspaces-app/src/redline/markup-margin.ts` | 718 | 9 | done |
| 23 | `meeting-task-capture.ts` + `meeting-capture-prompt.ts` + `meeting-capture-guards.ts` (split in A5) | 1348 | 6 | done |
| 24 | `meeting-notes-merge.ts` + `notes-ownership.ts` + `notes-section.ts` (split in A5) | 1067 | 5 | done |
| 25 | `deploy.ts` + `deploy-log.ts` (split in A7) | 1058 | 4 | done |
| 26 | `effort-task.ts` + `effort-calibration.ts` + `effort-format.ts` + `goal-effort.ts` (split in B5) | 1086 | 4 | done |
| 27 | `voice-resolve.ts` + `voice-status.ts` (split in A6) | 762 | 2 | done |
| 28 | `agent-notes.ts` + `note-redact.ts` (split in B8) | 671 | 2 | done |
| 29 | `recall-calendar.ts` + `google-oauth.ts` (split in A7) | 721 | 1 | done |
| 30 | `packages/workspaces-app/test/hub-render.test.ts` | 4078 | — | S |
| 31 | `voice-smooth.test.ts` + `voice-smooth-model.test.ts` (split in A8) | 729 | — | done |

The top eight are where the pain is: they carry 991 of the 1350 commits in this
queue. Rows 1 through 8 are worth filing as tickets now; below row 20 a split is
tidiness, and is best done by whoever is already editing the file.

---

## Files with no coverage, and why

A second kind of exception, kept here because it answers the same question the
rest of the page does: *this file breaks a bar, and here is the written reason.*
`bun run coverage` reports a per-package floor, not a per-file one, so nothing
mechanical fails on the rows below — they exist so that a zero in the per-file
listing is a decision on the record rather than an oversight.

One typographic quirk: the first row names its file as `mcp/src/mcp.ts`, with
the `packages/` prefix dropped on purpose. `loc-audit.ts` reads every
`packages/….ts` string anywhere in this document as a size-exception row, and
that file is 400 lines — spelling it in full would report a permanently stale
row at every run. Rows for files that ARE over the limit spell the path
normally.

| File | File lines | Why it has no test |
|---|---|---|
| `mcp/src/mcp.ts` | 400 | The boot file, and the one thing here that genuinely cannot be imported: it ends in `await server.connect(transport)`, so any test that loaded it would start an MCP server on the test runner's stdio and never return. Everything it used to hold has been lifted into modules that take their dependencies as arguments and are covered directly — `http-client`, `channel-messages`, `frame-handler`, `sse-loop`, `watch-registry`, `watch-restore`, `attachments` and `call-tool`, all at **100% line** coverage. Branch coverage across the eight runs 75–100%, lowest on `channel-messages` (75%), whose renderers carry a long tail of `??` fallbacks for fields older servers omit. What is left is construction and wiring: reading identity out of the environment, building the `Server`, and handing each module its collaborators in an order the language enforces. The wiring is asserted end to end instead, by the suites that spawn the shipped bundle as a child process and speak JSON-RPC to it (`packages/mcp/test/review-item-tools.test.ts`, `packages/server/test/mcp-durable-watches.test.ts`). |
| `packages/mcp/src/tool-schemas.ts` | 2012 | Declarative data with no branch in it: the 94-entry `tools/list` result. A test would restate the table. The one property worth asserting — that every advertised name has a dispatch arm — belongs to the dispatcher, not to the table. |
| `packages/mcp/src/tools/docs.ts`, `tools/tasks.ts`, `tools/workspace.ts` | 819 / 1070 / 528 | Not exempt, just not done: these are the three `CallTool` domain families, and the dispatcher that reaches them is now covered while the arms themselves are not. Each arm is a small translation from tool arguments to one REST call, so they are testable the moment someone writes the fakes; the coverage extraction stopped at the dispatcher to keep its diff a pure move. |
