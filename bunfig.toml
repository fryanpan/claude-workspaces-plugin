# Read by `bun test` from the repo root (the documented gate invocation).
# The preload makes descriptor exhaustion from concurrent worktree suite
# runs fail with a message that names contention, instead of surfacing as a
# bare EMFILE inside whichever test was running. See
# packages/server/test/fd-contention.ts.
[test]
preload = [
  # Ordered: the timing scale must be in the environment before any test file
  # imports the server, because the doc cadences resolve at module load.
  "./packages/server/test/timing.preload.ts",
  "./packages/server/test/fd-contention.preload.ts",
  # Every test server binds 127.0.0.1, so no other loopback listener can
  # answer a connect meant for it.
  "./packages/server/test/loopback-bind.preload.ts",
]
